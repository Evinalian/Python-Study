# 第 3 章 神经网络组件

## 学习目标

- 理解 `nn.Module` 作为所有网络层基类的设计原理
- 掌握常用层的参数含义、输入输出形状和使用场景
- 掌握激活函数的选择与适用场景
- 掌握损失函数的选择（回归 vs 分类 vs 二分类）
- 掌握参数初始化的常用策略及其对训练的影响
- 能独立搭建 MLP、CNN 和带残差连接的模块化网络
- 理解 BatchNorm vs LayerNorm 的深入差异
- 掌握 hooks 在调试和特征提取中的使用
- 了解 nn.ModuleDict、nn.ModuleList 和参数绑定的使用

---

## 前置知识

- 第 1 章 Tensor 与 autograd
- 第 2 章 Dataset / DataLoader
- 线性代数基础：矩阵乘法、卷积运算的直观理解
- 微积分：链式法则（反向传播的数学基础）

---

## 1. `nn.Module`：一切网络层的基类

在前两章中，我们已经学会了如何创建和操作 Tensor，以及如何用 autograd 计算梯度。但到目前为止，我们是用原始的张量运算和手动 `backward()` 来完成训练的——这在小规模实验中可行，但在实际项目中很快就失控了。想象一下，一个 ResNet-50 有数百个参数张量，你需要手动管理每个参数的 `requires_grad`、手动调用每个参数的梯度清零、手动组织前向传播的层次结构——这不仅是代码量的问题，更是正确性和可维护性的问题。

`nn.Module` 是 PyTorch 为解决这些问题而设计的核心抽象。它就像一个容器，把所有子层、参数和前向传播逻辑封装在一个统一的结构中。它的设计借鉴了面向对象编程的思想——每个网络组件（Layer、激活函数、归一化层）都是一个 Module，它们可以相互嵌套，形成树状结构。

PyTorch 中所有神经网络组件都继承自 `torch.nn.Module`。它将**参数管理**、**前向传播**、**梯度追踪**统一封装。

```python
import torch
import torch.nn as nn

class MyModel(nn.Module):
    def __init__(self):
        super().__init__()           # 必须调用父类 __init__
        # 在此定义子层（这些子层也是 nn.Module）
        self.linear = nn.Linear(10, 5)

    def forward(self, x):
        # 定义前向传播逻辑
        return self.linear(x)
```

### 1.1 `__init__` 与 `forward` 的分工

这对分工是 nn.Module 设计的精髓。`__init__` 做**声明式的参数注册**——定义网络中有哪些可学习的组件。PyTorch 通过 `__setattr__` 这个 Python 魔术方法自动追踪所有赋值为 `nn.Module` 类型的属性，将它们注册为子模块。这就是为什么你不需要手动维护一个"所有参数"的列表——Module 自动做了这件事。

`forward` 做**命令式的前向逻辑定义**——描述数据如何流经这些组件。你永远不要直接调用 `model.forward(x)`，而应该调用 `model(x)`。后者不仅调用 `forward`，还会在前后触发 PyTorch 的钩子机制（hooks），确保某些特性（如 Dropout 的 train/eval 模式切换、BatchNorm 的统计量更新）正常工作。

```python
model = MyModel()
x = torch.randn(4, 10)     # batch_size=4, input_dim=10
output = model(x)           # 正确：调用 __call__ -> forward + hooks
# output = model.forward(x) # 错误：跳过 hooks
```

如果你直接用 `model.forward(x)`，Dropout 在 eval 模式下仍然会生效（因为 hooks 没有被触发来禁用它），BatchNorm 可能使用错误的统计量。这个 bug 非常隐蔽，因为代码不会报错，但模型行为会异常。

### 1.2 `nn.Module` 提供的关键能力

nn.Module 不仅是一个基类，它提供了一整套管理神经网络生命周期的基础设施。以下是最常用的几个能力：

- **参数遍历**：`named_parameters()` 递归返回所有子模块的可学习参数，是 optimizer 初始化的数据源
- **设备迁移**：`model.to("cuda")` 递归将所有参数和 buffer 移到目标设备——一行代码完成全模型的设备迁移
- **模式切换**：`model.train()` / `model.eval()` 递归设置所有子模块的训练模式，决定 Dropout 是否生效、BatchNorm 是否更新统计量
- **结构查看**：`print(model)` 输出完整的模块树，是理解模型结构的首要工具

```python
model = MyModel()

# 遍历所有参数
for name, param in model.named_parameters():
    print(f"{name}: {param.shape}")

# 将所有参数移到 GPU
model = model.to("cuda")

# 切换训练/评估模式
model.train()    # 启用 Dropout、BatchNorm 更新统计量
model.eval()     # 禁用 Dropout、BatchNorm 使用固定统计量

# 查看模型结构
print(model)

# 查看所有参数的 device 和 dtype
for name, param in model.named_parameters():
    print(f"{name}: device={param.device}, dtype={param.dtype}")

# 获取所有子模块
for name, module in model.named_modules():
    print(f"{name}: {module.__class__.__name__}")

# 获取所有子模块（包括自身）
for name, module in model.named_children():
    print(f"{name}: {module.__class__.__name__}")
# named_children 只返回直接的子模块，不递归

# 冻结/解冻参数
for param in model.parameters():
    param.requires_grad = False   # 冻结所有参数

# 只冻结特定层
for param in model.linear.parameters():
    param.requires_grad = False
```

`named_modules()` vs `named_children()` 的区别：前者递归遍历所有后代模块（包括子模块的子模块），后者只返回直接子模块。在调试深层网络时，`named_modules()` 能帮你看到完整的结构树。

### 1.3 `nn.ModuleDict` 和 `nn.ModuleList`

当你需要用 **动态方式** 管理子模块（而非固定的属性名）时，普通的 Python list 或 dict 是不够的——它们的元素不会被 PyTorch 自动注册为子模块，这意味着 `model.parameters()` 不会返回这些层里的参数，`model.to("cuda")` 也不会把它们移到 GPU。

这就是 `nn.ModuleList` 和 `nn.ModuleDict` 的用武之地。它们是 Python list/dict 的 PyTorch 版本，会自动注册其中包含的 `nn.Module` 对象。

`nn.ModuleList` 的两个关键优势：参数被正确注册（可以被 optimizer 更新），以及索引访问的便利性。

```python
# nn.ModuleList: 像列表一样管理子模块
class DynamicNet(nn.Module):
    def __init__(self, layer_sizes):
        super().__init__()
        self.layers = nn.ModuleList()
        for i in range(len(layer_sizes) - 1):
            self.layers.append(nn.Linear(layer_sizes[i], layer_sizes[i+1]))
        # nn.ModuleList 正确注册了所有子模块参数

    def forward(self, x):
        for layer in self.layers[:-1]:
            x = torch.relu(layer(x))
        return self.layers[-1](x)

model = DynamicNet([10, 20, 30, 5])
print(model)
# All layers are properly registered

# 注意：如果使用普通 Python list，参数不会被注册！
class BrokenNet(nn.Module):
    def __init__(self):
        super().__init__()
        self.layers = [nn.Linear(10, 20), nn.Linear(20, 5)]
        # 参数不会被自动追踪！model.parameters() 会返回空
```

`BrokenNet` 是一个经典的陷阱——代码不会报错，但训练时 optimizer 不会更新任何参数，你排查半天才发现参数根本没有被注册。记住：**永远不要用普通 Python list/dict 存储 `nn.Module` 对象**。

`nn.ModuleDict` 进一步支持按名称访问子模块的功能，适合多任务学习（不同任务有不同的输出头）或模型集成等场景。

```python
# nn.ModuleDict: 像字典一样管理子模块
class MultiTaskNet(nn.Module):
    def __init__(self, shared_dim, task_dims):
        super().__init__()
        self.shared = nn.Linear(shared_dim, 256)

        # 不同任务的输出头
        self.heads = nn.ModuleDict()
        for task_name, output_dim in task_dims.items():
            self.heads[task_name] = nn.Linear(256, output_dim)

    def forward(self, x, task_name):
        shared_features = torch.relu(self.shared(x))
        return self.heads[task_name](shared_features)

model = MultiTaskNet(128, {"classification": 10, "regression": 1})
print(model.heads.keys())  # odict_keys(['classification', 'regression'])

# 通过名称访问
cls_output = model(torch.randn(4, 128), "classification")
```

多任务网络的实际应用场景很广泛——比如一个电商推荐系统同时预测"用户是否会点击"（分类）和"用户会看多久"（回归），共享的底层特征提取器学习通用的用户和商品表示，不同任务的输出头各自学习任务特定的映射。

### 1.4 参数绑定（Tied Weights / Weight Sharing）

参数绑定是深度学习中一个优雅但常被忽略的技术。核心思想是：**让两个不同的层共享同一组参数**。这样做有几个目的：
1. **减少参数量**：自动编码器中编码器和解码器共享权重，参数量减半
2. **引入归纳偏置**：语言模型中输入 embedding 和输出 projection 共享权重，使语义相近的词在输入和输出空间中有类似的距离关系
3. **防止过拟合**：在数据稀疏的任务中，共享参数减少了模型自由度

```python
class TiedAutoEncoder(nn.Module):
    """编码器和解码器共享权重"""

    def __init__(self, input_dim, hidden_dim):
        super().__init__()
        self.encoder = nn.Linear(input_dim, hidden_dim)
        self.decoder = nn.Linear(hidden_dim, input_dim)

        # 绑定权重：解码器权重 = 编码器权重的转置
        self.decoder.weight = nn.Parameter(self.encoder.weight.T)

        # 注意：绑定后 decoder.weight 和 encoder.weight 是不同对象，
        # 但初始值相同。如果要训练时保持绑定，需要额外处理。

    def forward(self, x):
        hidden = torch.relu(self.encoder(x))
        reconstructed = self.decoder(hidden)
        return reconstructed


# 更严格的绑定：始终共享
class StrictTiedAutoEncoder(nn.Module):
    """严格共享权重：decoder 不独立拥有 weight"""

    def __init__(self, input_dim, hidden_dim):
        super().__init__()
        self.encoder = nn.Linear(input_dim, hidden_dim, bias=False)
        self.decoder_bias = nn.Parameter(torch.zeros(input_dim))
        # decoder 的 weight 直接从 encoder.weight 转置获取

    def forward(self, x):
        hidden = torch.relu(self.encoder(x))         # W @ x
        # 解码时直接使用 encoder.weight 的转置
        reconstructed = x @ self.encoder.weight + self.decoder_bias
        return reconstructed
```

第一种方式（`nn.Parameter(self.encoder.weight.T)`）在初始化时共享了权重的值，但在训练过程中 `decoder.weight` 和 `encoder.weight.T` 是独立更新的，绑定会逐渐失效。第二种方式（在 `forward` 中直接用 `self.encoder.weight`）确保了严格的权重共享——不存在独立的 `decoder.weight`，所以训练过程中的绑定是自动保证的。

---

## 2. 常用层详解

有了 nn.Module 的基础设施，我们来看具体的网络层。每一层的介绍都遵循相同的逻辑：先讲**这个层解决什么问题**，再讲**它的数学运算和参数**，最后看**适用的场景和注意事项**。

### 2.1 `nn.Linear` —— 全连接层

全连接层（也叫密集层或线性层）是最基础的神经网络组件。它的功能可以一言以蔽之：**对输入做线性变换** `y = xW^T + b`。

在深度学习中，全连接层通常扮演两个角色：
1. **特征空间变换**：将输入特征从 `in_features` 维映射到 `out_features` 维，实现升维或降维
2. **最终分类/回归头**：将提取的特征映射到输出空间（类别数或回归值）

全连接层的参数量是 `in_features * out_features + out_features`。对于图像任务，如果直接将高维像素扁平化后接全连接层，参数量会爆炸——这也是 CNN 被发明出来的核心动机之一（卷积通过参数共享大幅减少参数量）。

```python
import torch.nn as nn

# nn.Linear(in_features, out_features, bias=True)
layer = nn.Linear(10, 5)   # 输入 10 维 -> 输出 5 维

x = torch.randn(32, 10)    # (batch_size, in_features)
y = layer(x)               # (32, 5)

print(layer.weight.shape)  # torch.Size([5, 10]) — W: (out, in)
print(layer.bias.shape)    # torch.Size([5])     — b: (out,)
```

内部运算：`y = x @ W.T + b`，即 `y[i, j] = sum_k (x[i, k] * W[j, k]) + b[j]`。

**参数数量**：`in_features * out_features + out_features`。

**权重矩阵形状详解**：

权重矩阵的形状 `(out_features, in_features)` 是 PyTorch 的约定。这与许多教科书上 `y = Wx + b` 中 W 为 `(out, in)` 的写法一致，但需要注意：
- 输入向量 x 的形状是 `(in_features,)`
- 输出向量 y 的形状是 `(out_features,)`
- 矩阵乘法 `x @ W.T` 中，`x` 是 `(batch, in)`，`W.T` 是 `(in, out)`，结果 `(batch, out)`

```
输入:  x,  shape = (batch, in_features)   例如 (32, 10)
权重:  W,  shape = (out_features, in_features)  权重在右边做转置
偏置:  b,  shape = (out_features,)
输出:  y = x @ W.T + b,  shape = (batch, out_features)  例如 (32, 5)

矩阵乘法维度:
   (32, 10) @ (10, 5) = (32, 5)
       x    @   W.T   =   y
```

> Linear 层的权重矩阵形状是 `(out_features, in_features)` 而非 `(in_features, out_features)`。这与数学课本上 `y = Wx + b` 中 `W` 是 `(out, in)` 的约定一致，但容易与某些框架混淆。

### 2.2 `nn.Conv2d` —— 二维卷积层

卷积层是计算机视觉领域的核心组件。它解决了全连接层处理图像时的核心痛点：**空间信息的破坏和参数量的爆炸**。

考虑一张 224x224 的 RGB 图像（共 150,528 个像素）。如果使用全连接层直接映射到 256 维的隐藏空间，仅这一层的参数就是 `150528 * 256 = 38,535,168` 个——约 1.54 亿字节（fp32）。而一个 3x3 卷积从 3 通道到 256 通道只有 `3 * 256 * 3 * 3 = 6,912` 个参数。这是卷积的"参数共享"特性带来的巨大优势。

卷积的另一个核心优势是**平移等变性**（translation equivariance）：如果输入图像中的物体移动了，卷积输出的特征图也会相应地移动。这使得卷积网络能够识别出现在图像不同位置的同一个物体，而不需要针对每个位置学习独立的检测器。

```python
# nn.Conv2d(in_channels, out_channels, kernel_size, stride=1, padding=0)
conv = nn.Conv2d(
    in_channels=3,          # 输入通道数（RGB 图像 = 3）
    out_channels=16,        # 输出通道数（卷积核数量）
    kernel_size=3,          # 卷积核大小 3x3
    stride=1,               # 步长
    padding=1,              # 填充（保持空间尺寸不变: padding = kernel_size // 2）
)

x = torch.randn(8, 3, 32, 32)  # (batch, channels, height, width)
y = conv(x)                     # (8, 16, 32, 32)

print(conv.weight.shape)        # torch.Size([16, 3, 3, 3])
print(conv.bias.shape)          # torch.Size([16])
```

**卷积运算的维度变化图解**：

```
输入:  (batch=8, in_c=3,  H=32, W=32)
                         |
卷积核: (out_c=16, in_c=3, kH=3, kW=3)
                         |
输出:  (batch=8, out_c=16, H'=32, W'=32)  (当 stride=1, padding=1 时 H'=H)
```

**多通道卷积详解**：

一个关键的理解是：**每个输出通道是所有输入通道的加权和**。对于 16 个输出通道，每个通道有独立的 3 通道卷积核，其输出是 3 个输入通道各自卷积结果的累加。这意味着一层卷积不仅在提取空间特征（边缘、纹理等），同时也在进行**跨通道的信息融合**。

```
对于每个输出通道 j (共 16 个):
    output[j] = sum_i (input[i] 卷积 kernel[j, i]) + bias[j]
                         3 个通道
```

**输出尺寸计算公式**：

```
H_out = floor((H_in + 2 * padding - dilation * (kernel_size - 1) - 1) / stride) + 1
W_out = floor((W_in + 2 * padding - dilation * (kernel_size - 1) - 1) / stride) + 1
```

**保持尺寸不变的常用配置**：

`padding = kernel_size // 2` 是 PyTorch 中一个非常有用的经验公式。对于奇数尺寸的卷积核（3, 5, 7），`kernel_size // 2` 的 padding 能够恰好保持空间尺寸不变（当 stride=1 时）。这让你可以在不担心尺寸变化的情况下堆叠多层卷积。

| kernel_size | padding | dilation | 效果 |
|-------------|---------|----------|------|
| 3 | 1 | 1 | "same" padding，输出尺寸不变 (stride=1) |
| 5 | 2 | 1 | "same" padding，输出尺寸不变 (stride=1) |
| 7 | 3 | 1 | "same" padding，输出尺寸不变 (stride=1) |
| 1 | 0 | 1 | 1x1 卷积，只改变通道数，不改空间尺寸 |

**参数量**：`out_channels * in_channels * kernel_size^2 + out_channels`。

**卷积尺寸计算示例**：

当你设计 CNN 架构时，手动跟踪每层输出的空间尺寸是必要的基本功。以下以经典的 MNIST 网络为例展示计算过程：

```python
# 示例：计算每层输出的空间尺寸
# 输入: (1, 28, 28) MNIST 灰度图

# Conv1: in=1, out=32, k=3, p=1, s=1
# H' = (28 + 2*1 - 3)/1 + 1 = 28  (输出: 32, 28, 28)

# MaxPool: k=2, s=2
# H' = 28/2 = 14  (输出: 32, 14, 14)

# Conv2: in=32, out=64, k=3, p=1, s=1
# H' = (14 + 2*1 - 3)/1 + 1 = 14  (输出: 64, 14, 14)

# MaxPool: k=2, s=2
# H' = 14/2 = 7  (输出: 64, 7, 7)

# Flatten -> 64 * 7 * 7 = 3136
# Linear(3136, 128) -> Linear(128, 10)
```

### 2.3 感受野 (Receptive Field) 计算

卷积网络中，每层输出的每个元素在原始输入中"看到"的区域称为感受野。理解感受野有助于架构设计——比如你需要多少层才能让网络的最高层"看到"足够的上下文。

```
感受野递推公式：
RF_{out} = RF_{in} + (kernel_size - 1) * stride_prod

其中 stride_prod 是之前所有层的 stride 乘积。

示例：两个 3x3 卷积，stride=1
第 1 层：RF = 1 + (3-1)*1 = 3x3
第 2 层：RF = 3 + (3-1)*1 = 5x5

两个 3x3 卷积的等效感受野 = 一个 5x5 卷积
但参数量：2*3*3=18 vs 5*5=25，且两个 3x3 有更多非线性
```

这个分析揭示了为什么 VGG 网络用多个 3x3 卷积替代大卷积核：参数量更少，非线性更多（每个 3x3 后有 ReLU），而感受野是等效的。这是一个经典的"深度学习设计的 trade-off 智慧"。

### 2.4 1x1 卷积的作用

1x1 卷积看似无用——一个只看一个像素的卷积能提取什么空间信息？但它的价值不在于空间特征提取，而在于**通道维度的线性变换**。

1x1 卷积等价于对每个空间位置的所有通道做一次 Linear 变换（矩阵乘法）。具体来说，`Conv2d(C_in, C_out, 1)` 就是为每个 `(h, w)` 位置执行 `y[h,w,:] = x[h,w,:] @ W + b`。这使得它在以下场景中非常有用：

- **通道降维**：减少后续大卷积的计算量（ResNet bottleneck 的经典设计）
- **通道升维**：增加网络的表达能力
- **跨通道信息融合**：在不引入空间偏置的情况下混合不同通道的信息

```python
# 1. 通道数升降维（降维减少参数量，升维增加表达能力）
conv_1x1_reduce = nn.Conv2d(256, 64, kernel_size=1)   # 降维
conv_1x1_expand = nn.Conv2d(64, 256, kernel_size=1)   # 升维

# 2. 跨通道信息融合（每个空间位置的各通道做线性组合）
# 等价于对每个像素的所有通道做一次 Linear 变换

# 3. 在瓶颈结构中减少计算量（ResNet Bottleneck）
class Bottleneck(nn.Module):
    def __init__(self, in_c, mid_c, out_c):
        super().__init__()
        # 1x1 降维 -> 3x3 卷积 -> 1x1 升维
        self.conv1 = nn.Conv2d(in_c, mid_c, kernel_size=1)    # 降维
        self.conv2 = nn.Conv2d(mid_c, mid_c, kernel_size=3, padding=1)
        self.conv3 = nn.Conv2d(mid_c, out_c, kernel_size=1)   # 升维

    def forward(self, x):
        identity = x
        out = torch.relu(self.conv1(x))
        out = torch.relu(self.conv2(out))
        out = self.conv3(out)
        return torch.relu(out + identity)
```

Bottleneck 结构的核心洞察是：通过中间的 1x1 降维卷积将通道数压缩（如从 256 到 64），昂贵的 3x3 卷积只需要在 64 通道上操作，计算量大幅减少；然后再通过 1x1 升维恢复到原始通道数以匹配残差连接。这种"压缩-处理-恢复"的模式在 ResNet-50/101/152 中广泛使用。

### 2.5 RNN 系列 —— 序列建模

当数据具有**时序依赖**（如文本、语音、股票价格）时，全连接层和卷积层都不够用——它们假设每个输入样本是独立的，无法建模样本之间的时间关系。

RNN（Recurrent Neural Network）通过引入隐藏状态（hidden state）来解决这个问题：每个时间步的计算不仅依赖于当前输入，还依赖于上一时间步的隐藏状态。这形成了一个循环结构，理论上可以捕获任意长度的依赖关系。

然而，朴素的 RNN 面临着**梯度消失/爆炸**的严重问题。当序列很长时，反向传播需要穿过很多时间步，梯度在连乘过程中指数衰减（消失）或增长（爆炸）。LSTM 和 GRU 通过引入门控机制（gating mechanism）来缓解这一问题。

```python
# nn.LSTM(input_size, hidden_size, num_layers=1, batch_first=False, bidirectional=False)
# 默认 batch_first=False: input shape (seq_len, batch, input_size)
# 设置 batch_first=True: input shape (batch, seq_len, input_size)

lstm = nn.LSTM(
    input_size=128,           # 输入特征维度
    hidden_size=256,          # 隐藏状态维度
    num_layers=2,             # LSTM 层数
    batch_first=True,         # 第一维是 batch
    bidirectional=True,       # 双向 LSTM
)

# 输入: (batch, seq_len, input_size)
x = torch.randn(16, 50, 128)
output, (h_n, c_n) = lstm(x)

print(output.shape)  # (16, 50, 512) — hidden_size * 2（因为 bidirectional）
print(h_n.shape)     # (4, 16, 256)    — D * num_layers, batch, hidden_size
print(c_n.shape)     # (4, 16, 256)    — D * num_layers, batch, hidden_size
```

`batch_first=True` 这个参数值得特别注意。PyTorch 的 RNN 默认使用 `batch_first=False`（即 `(seq_len, batch, feature)` 的排列），这与 PyTorch 其他部分的 `(batch, ...)` 惯例不一致，是历史遗留问题。建议始终设置 `batch_first=True` 以保持一致性。

双向 LSTM 的输出维度是 `hidden_size * 2`——前向 LSTM 产生 256 维，后向 LSTM 也产生 256 维，拼接后就是 512 维。这使得每个时间步的输出同时包含了"上文"和"下文"的信息。

**LSTM 门控机制详解**：

LSTM 的核心创新是 **Cell State（细胞状态）**。你可以把它想象为一条贯穿所有时间步的"信息高速公路"——门控制什么信息进入这个高速公路（输入门）、什么信息从高速公路上丢弃（遗忘门）、以及从高速公路上提取什么信息输出（输出门）。

```
输入门 i_t:   控制新信息写入 cell state 的量
遗忘门 f_t:   控制旧 cell state 保留的量
输出门 o_t:   控制从 cell state 输出到 hidden state 的量

公式（简化）：
i_t = sigmoid(W_i * [h_{t-1}, x_t] + b_i)      # 输入门
f_t = sigmoid(W_f * [h_{t-1}, x_t] + b_f)      # 遗忘门
o_t = sigmoid(W_o * [h_{t-1}, x_t] + b_o)      # 输出门
g_t = tanh(W_g * [h_{t-1}, x_t] + b_g)         # 候选值
c_t = f_t * c_{t-1} + i_t * g_t                # cell state 更新
h_t = o_t * tanh(c_t)                           # hidden state 输出
```

遗忘门的引入是 LSTM 相比朴素 RNN 的最大改进。当 `f_t` 接近 1 时，旧的 cell state 被完整保留——这意味着梯度在反向传播时可以直接通过 cell state 传递而不衰减。这就是 LSTM 能处理长序列的根本原因。

**Hidden State vs Cell State**：

很多初学者混淆这两个状态，但它们的角色完全不同。Hidden state 是"对外输出"——它被传给下一层或用于做预测。Cell state 是"内部记忆"——它不直接对外暴露，只在 LSTM 层内部传递。

| 状态 | 作用 | 用途 |
|------|------|------|
| Hidden State (h_t) | 短期记忆，当前时刻的输出 | 传给下一层/做预测 |
| Cell State (c_t) | 长期记忆，携带跨时间步的信息 | 仅在 LSTM 层间传递 |

**双向 LSTM 输出拼接**：

```python
# 双向 LSTM 的前向和后向输出拼接
lstm = nn.LSTM(128, 256, bidirectional=True, batch_first=True)
x = torch.randn(16, 50, 128)
output, (h_n, c_n) = lstm(x)

# output shape: (batch, seq_len, 2 * hidden_size) = (16, 50, 512)
# output 的前 256 维是前向 LSTM 输出，后 256 维是后向 LSTM 输出

# 分离前向和后向
forward_output = output[:, :, :256]     # 前 256 维
backward_output = output[:, :, 256:]    # 后 256 维
```

**RNN vs LSTM vs GRU 的选择**：

在实践中，GRU 往往是一个很好的默认选择——它的效果接近 LSTM，但参数更少、训练更快。随着 Transformer 的兴起，很多序列建模任务（尤其是 NLP）已经从 RNN 转到了 Transformer，但在某些时序预测和资源受限的场景中，LSTM/GRU 仍然有竞争力。

| 特性 | RNN | LSTM | GRU |
|------|-----|------|-----|
| 门控机制 | 无 | 输入门/遗忘门/输出门 | 重置门/更新门 |
| 长期依赖 | 差（梯度消失） | 好 | 好 |
| Cell State | 无 | 有 | 无（合并到 hidden） |
| 参数量 | 少 | 多（~4x RNN） | 中（~3x RNN） |
| 训练速度 | 快 | 慢 | 中等 |
| 推荐度 | 几乎不推荐 | 经典选择 | 参数更少，效果接近 LSTM |

### 2.6 Transformer 层（简要）

Transformer 自 2017 年提出以来已彻底改变了深度学习格局。其核心创新——自注意力机制（Self-Attention）——让模型能够直接建模序列中任意两个位置之间的关系，而不需要通过 RNN 的逐步传递。PyTorch 内置了 Transformer 的基本组件。

```python
# nn.TransformerEncoderLayer(d_model, nhead, dim_feedforward, dropout)
encoder_layer = nn.TransformerEncoderLayer(
    d_model=512,              # 模型维度
    nhead=8,                  # 注意力头数
    dim_feedforward=2048,     # FFN 中间维度
    dropout=0.1,
    batch_first=True,         # (batch, seq, d_model)
)

x = torch.randn(32, 100, 512)  # (batch, seq_len, d_model)
y = encoder_layer(x)            # (32, 100, 512)
```

Transformer 的核心组件将在后续章节深入讲解。此处只需了解其接口形态。值得注意的是，`TransformerEncoderLayer` 的默认 `batch_first=False` 与 LSTM 的默认行为一致——需要显式设置 `batch_first=True`。

### 2.7 归一化层

归一化层（Normalization Layers）是深度学习中最重要但不直观的技术之一。它们的共同目标都是：**将激活值标准化为均值为 0、方差为 1 的分布**，从而稳定训练过程、加速收敛。

但不同的归一化在**什么维度上计算统计量**是不同的。这个差异看起来细微，却决定了每种归一化适合什么场景。

#### BatchNorm

BatchNorm 是计算机视觉中最常用的归一化方式。它在**batch 维度**上计算均值和方差——对同一个通道（channel）的所有空间位置和所有 batch 样本求平均。这使得统计量包含了"跨样本的信息"，因此 batch 越大，统计量越稳定。

```python
# BatchNorm1d: 对全连接层的输出归一化
bn1d = nn.BatchNorm1d(num_features=128)

# 输入: (batch, features) 或 (batch, features, L)
x = torch.randn(32, 128)
y = bn1d(x)  # (32, 128) — 沿 batch 维度计算均值/方差

# BatchNorm2d: 对卷积层的输出归一化
bn2d = nn.BatchNorm2d(num_features=64)
x = torch.randn(8, 64, 32, 32)  # (batch, channels, H, W)
y = bn2d(x)                      # (8, 64, 32, 32)
# 在 (batch, H, W) 维度上计算均值和方差，每个通道独立
```

**BatchNorm 的计算**：

BatchNorm 有两个可学习参数：`gamma`（缩放）和 `beta`（平移）。初始时 `gamma=1, beta=0`，所以归一化后的输出一开始就是标准化的。随着训练进行，网络可以学习调整这两个参数来恢复被标准化消除的有用信息。

```
对于每个通道 c：
    mean_c = mean(x[:, c, :, :])   # 沿 (batch, H, W)
    var_c  = var(x[:, c, :, :])    # 沿 (batch, H, W)
    x_hat[:, c, :, :] = (x[:, c, :, :] - mean_c) / sqrt(var_c + eps)
    y[:, c, :, :] = gamma_c * x_hat[:, c, :, :] + beta_c
```

BatchNorm 在训练和推理时的行为不同，这是一个重要的细节：
- **训练时**：使用当前 batch 的统计量（mean, var）做归一化，同时用指数移动平均更新 `running_mean` 和 `running_var`
- **推理时**：使用训练时累积的 `running_mean` 和 `running_var` 做归一化，保证确定性

#### LayerNorm

LayerNorm 是 NLP（特别是 Transformer）中主流的归一化方式。它在**特征维度**上计算均值和方差——对每个样本的每个位置的所有特征求平均。这使得每个样本的归一化完全独立，不受 batch 中其他样本的影响。

```python
ln = nn.LayerNorm(normalized_shape=512)
x = torch.randn(32, 100, 512)     # (batch, seq, features)
y = ln(x)                          # (32, 100, 512)
# 沿最后一个维度 (features) 计算均值和方差
```

**LayerNorm 的计算**：

```
对于每个样本的每个位置：
    mean = mean(x[b, s, :])       # 沿 feature 维度
    var  = var(x[b, s, :])        # 沿 feature 维度
    x_hat = (x - mean) / sqrt(var + eps)
    y = gamma * x_hat + beta
```

**BatchNorm vs LayerNorm 的深入对比**：

| 维度 | BatchNorm | LayerNorm |
|------|-----------|-----------|
| 统计维度 | 沿 (batch, H, W) — 对同一通道的所有激活 | 沿 (features) — 对同一样本的所有特征 |
| 每个样本独立 | 否（依赖 batch 中其他样本） | 是 |
| 依赖 batch_size | 强依赖（小 batch 统计量不稳定） | 完全不依赖 |
| 训练/推理差异 | 有（训练用 batch 统计量，推理用 running stats） | 无（训练和推理行为完全一致） |
| 适用场景 | CNN（CV 任务，batch 较大） | Transformer、RNN（NLP 任务） |
| 序列建模 | 不适合变长序列 | 天然适合变长序列 |
| 对小 batch 的鲁棒性 | 差 | 好 |

**为什么 CV 用 BatchNorm，NLP 用 LayerNorm**：

这是业界经过大量实验得出的经验结论，背后有理论依据：

- **CV 中**：同一个通道的不同空间位置共享相同的统计特性（颜色分布一致），所以可以跨 batch 统计；且 CV 的 batch_size 通常较大（32-256），统计量稳定。此外，CNN 特征图中的不同通道代表不同的视觉概念（边缘、纹理、形状），它们的分布特性确实不同，各自独立归一化是有意义的。
- **NLP 中**：每个 token 的 embedding 维度内的特征含义完全不同（维度 0 可能代表"词性"，维度 1 可能代表"时态"），跨 batch 统计没有意义；且 NLP 中 batch 通常较小（受序列长度限制），batch 统计不稳定。LayerNorm 在每个样本内归一化，完美避开了这些限制。

**其他归一化变体**：

```python
# InstanceNorm: 对每个样本的每个通道独立归一化
# 用于风格迁移等每个样本独立处理的场景
in2d = nn.InstanceNorm2d(num_features=64)

# GroupNorm: 将通道分组，对每组归一化
# 在通道数之间折衷，适用于小 batch 的 CV 任务
gn = nn.GroupNorm(num_groups=32, num_channels=256)
```

GroupNorm 是 BatchNorm 和 InstanceNorm 的折中：如果 `num_groups=1`，等价于 LayerNorm；如果 `num_groups=num_channels`，等价于 InstanceNorm。通常设 32 个 group，在检测和分割等 batch_size 较小的任务中效果很好。

### 2.8 Dropout —— 正则化

Dropout 是深度学习中最经典的正则化技术之一。它的原理极其简单但效果显著：**在训练时，以概率 p 随机将一部分神经元的输出置为零**。

为什么这样有效？我们可以从两个角度理解：

1. **集成学习视角**：每次 dropout 随机"杀死"一部分神经元，等效于训练一个不同的子网络。多个 epoch 下来，相当于训练了大量子网络的集成——最终的推理模型（不 dropout）近似于这些子网络的平均。

2. **特征共适应视角**：dropout 阻止了神经元之间形成"共适应"关系（即一个神经元过度依赖另一个神经元的存在）。每个神经元必须学会在任意其他神经元可能"缺勤"的情况下独立做出有用贡献。

```python
dropout = nn.Dropout(p=0.5)    # 50% 概率将元素置零

x = torch.ones(10, 20)
# 训练模式
dropout.train()
y_train = dropout(x)
print((y_train == 0).float().mean().item())  # 约 0.5

# 推理模式
dropout.eval()
y_eval = dropout(x)
print((y_eval == 0).float().mean().item())   # 0 — eval 模式下不 dropout
```

**Dropout 的自动缩放机制**：

Dropout 在训练时会自动将保留的神经元输出乘以 `1/(1-p)`。这被称为"反向 Dropout"或"inverted dropout"。为什么需要这个缩放？

```
训练时：保留的神经元输出 * 1/(1-p) 以保持期望不变
  期望: E[y] = p*0 + (1-p)*(x/(1-p)) = x  (期望保持不变)

推理时：不做任何操作（恒等映射）
```

如果没有这个缩放，训练时输出的期望只有原来的 `(1-p)` 倍。当你切换到推理模式（不 dropout）时，输出的尺度会与训练时不匹配，导致效果下降。这个缩放让训练和推理时的激活值期望保持一致，使得模式切换对输出尺度的冲击最小。

**常用 Dropout 变体**：

```python
# 标准 Dropout：随机置零
dropout = nn.Dropout(p=0.5)

# Dropout2d：对 (C, H, W) 整个通道随机置零
# 用于 CNN 的通道级 dropout
dropout2d = nn.Dropout2d(p=0.2)

# AlphaDropout：保持均值和方差的 Dropout
# 配合 SELU 激活函数使用
alpha_dropout = nn.AlphaDropout(p=0.1)
```

`Dropout2d` 的行为与标准 Dropout 有本质区别：它不是随机置零单个像素，而是随机置零整个通道（即该通道的所有空间位置都被清零）。这模拟了"某个特征图整体不可用"的情况，比像素级 dropout 更适合 CNN 的通道语义。

---

## 3. 激活函数

### 3.0 激活函数为什么必不可少

如果网络中只有线性层（Linear、Conv），无论堆叠多少层，整体仍然是一个线性变换——多层线性变换的复合仍然是线性的。证明很简单：`f(g(x)) = (W2 @ (W1 @ x + b1) + b2) = (W2@W1) @ x + (W2@b1+b2)`，本质上还是 `W'x + b'` 的形式。

激活函数通过引入**非线性**打破了这个限制。它让网络能够学习到比线性函数更复杂的映射关系——分类边界不再是直线、回归曲线不再是平面。

选择正确的激活函数对训练的速度、稳定性和最终效果都有巨大影响。以下是常用激活函数的速览：

```python
# 各激活函数与其适用场景

relu = nn.ReLU()        # max(0, x) — 最常用，缓解梯度消失，CNN/MLP 首选
gelu = nn.GELU()        # x * Phi(x) — Transformer 标配，比 ReLU 平滑
sigmoid = nn.Sigmoid()  # 1/(1+e^{-x}) → [0,1] — 二分类输出层
tanh = nn.Tanh()        # (e^{2x}-1)/(e^{2x}+1) → [-1,1] — RNN 隐藏层
softmax = nn.Softmax(dim=-1)  # exp(x_i)/sum(exp(x_j)) — 多分类输出
```

### 选择指南

| 激活函数 | 输出范围 | 适用场景 | 注意事项 |
|----------|----------|----------|----------|
| ReLU | [0, +inf) | CNN/MLP 隐藏层 | 输入为负时梯度为 0（dying ReLU） |
| LeakyReLU | (-inf, +inf) | 替代 ReLU，缓解 dying | 负半轴有小斜率(默认 0.01) |
| GELU | (-inf, +inf) | Transformer、BERT/GPT | 比 ReLU 计算稍慢 |
| Sigmoid | (0, 1) | 二分类输出、门控 | 饱和区梯度接近 0，隐藏层不推荐 |
| Tanh | (-1, 1) | RNN 隐藏层 | 同 Sigmoid 有饱和问题 |
| Softmax | (0, 1) 且和为 1 | 多分类输出层 | 仅在输出层使用 |
| SiLU/Swish | (-inf, +inf) | 某些高效 CNN | x * sigmoid(x) |
| ELU | (-alpha, +inf) | 缓解 dying ReLU | 负半轴平滑，计算稍慢 |
| SELU | 自归一化 | 配合特定初始化 | 要求 LecunNormal 初始化 + AlphaDropout |

选择激活函数的决策树大致是：
- 分类输出层：二分类用 Sigmoid，多分类用 Softmax，多标签用 Sigmoid（每个标签独立）
- 隐藏层：CNN/MLP 首选 ReLU，Transformer 首选 GELU
- 如果 ReLU 出现大量 dying 神经元（输出始终为 0），换 LeakyReLU 或 ELU

### ReLU 变体详解

ReLU 的最大问题是"死亡神经元"（dying ReLU）：一旦某个神经元对所有输入都输出负值，其梯度恒为 0，参数永远不再更新，该神经元永久失效。这在两种情况下容易发生：(1) 学习率太大导致参数一次更新后就进入全负区域；(2) 初始化不当导致初始激活值就偏负。

```python
x = torch.tensor([-3.0, -1.0, 0.0, 1.0, 3.0])

# ReLU: max(0, x)
print(f"ReLU:      {nn.ReLU()(x)}")       # [0., 0., 0., 1., 3.]

# LeakyReLU: max(alpha*x, x)
print(f"LeakyReLU: {nn.LeakyReLU(0.1)(x)}")  # [-0.3, -0.1, 0., 1., 3.]

# PReLU: 可学习的 alpha
prelu = nn.PReLU()
print(f"PReLU:     {prelu(x)}")           # alpha 可学习

# ELU: x if x>0 else alpha*(exp(x)-1)
print(f"ELU:       {nn.ELU()(x)}")        # 负半轴平滑

# GELU: x * Phi(x)，其中 Phi 是标准正态 CDF
print(f"GELU:      {nn.GELU()(x)}")
# GELU 近似: 0.5*x*(1 + tanh(sqrt(2/pi)*(x + 0.044715*x^3)))
```

LeakyReLU 通过给负半轴一个微小的斜率（默认 0.01）来避免"完全死亡"的问题。即使输入为负，梯度至少是 0.01，参数仍然有微弱的更新信号。PReLU 进一步将这个斜率设为可学习的参数——网络自己决定负半轴的最佳斜率。

GELU 是 Transformer 时代的"明星激活函数"。它的数学形式 `x * Phi(x)`（Phi 是标准正态分布的 CDF）意味着：输入 x 乘以"x 大于随机噪声的概率"。这比 ReLU 的硬阈值（小于 0 就一刀切）更平滑，实际效果也更好。

```python
# 展示 dying ReLU 问题
x = torch.tensor([-1.0, 0.0, 1.0], requires_grad=True)
y = nn.ReLU()(x)
y.sum().backward()
print(x.grad)  # tensor([0., 0., 1.]) — 负输入梯度为 0
# 如果大量神经元长期输出负值，其梯度始终为 0，参数不再更新 —— "死亡"
```

### 激活函数的数学可视化

一张图胜过千言万语。以下代码绘制了五种激活函数的曲线，让你直观感受它们的形状差异。

```python
import matplotlib.pyplot as plt
import torch

x = torch.linspace(-5, 5, 200)
activations = {
    "ReLU": nn.ReLU()(x).numpy(),
    "LeakyReLU(0.1)": nn.LeakyReLU(0.1)(x).numpy(),
    "GELU": nn.GELU()(x).numpy(),
    "Sigmoid": torch.sigmoid(x).numpy(),
    "Tanh": torch.tanh(x).numpy(),
}

fig, axes = plt.subplots(1, 5, figsize=(15, 3))
for ax, (name, y) in zip(axes, activations.items()):
    ax.plot(x.numpy(), y)
    ax.set_title(name)
    ax.grid(True)
    ax.set_xlabel("x")
    ax.set_ylabel("f(x)")
plt.tight_layout()
plt.show()
```

---

## 4. 损失函数

损失函数是模型训练的目标函数——它定义了"什么样的预测是好的"。在数学上，损失函数将模型的预测和真实标签映射为一个标量值，优化器通过最小化这个值来训练模型。

选择损失函数的核心原则是：**损失函数应该反映你对"好预测"的定义**。对于回归任务，你希望预测值和真实值接近，所以用 MSE 或 MAE；对于分类任务，你希望正确类别的概率尽可能大，所以用交叉熵；对于排序任务，你希望正样本的距离小于负样本的距离，所以用 Triplet Loss。

### 4.1 `nn.MSELoss` —— 回归

MSE（Mean Squared Error，均方误差）是最经典的回归损失函数。它计算预测值与真实值之差的平方的平均值。对异常值敏感——因为误差被平方了，所以一个异常的大误差会对 loss 产生不成比例的影响。

```python
criterion = nn.MSELoss()

pred = torch.tensor([2.5, 0.0, 2.1])
target = torch.tensor([3.0, -0.5, 2.0])

loss = criterion(pred, target)
# loss = mean((pred - target)^2) = mean([0.25, 0.25, 0.01]) = 0.17
print(loss)
```

适用：房价预测、温度预测等连续值回归任务。

**MSE 的变体**：

```python
# L1Loss / MAE: 平均绝对误差（对异常值更鲁棒）
mae = nn.L1Loss()(pred, target)
print(f"MAE: {mae}")

# SmoothL1Loss: Huber Loss，结合 MSE 和 MAE 的优点
# 在误差小时用 MSE（平滑），误差大时用 MAE（鲁棒）
smooth_l1 = nn.SmoothL1Loss()(pred, target)
print(f"SmoothL1: {smooth_l1}")
```

MSE vs MAE vs SmoothL1 的选择取决于数据中异常值的分布：
- 如果数据干净（少有异常值），MSE 收敛更快
- 如果数据中混有大量异常值，MAE 更鲁棒（不会被个别大误差主导梯度）
- SmoothL1 是两者的折中：小误差时像 MSE（平滑、导数连续），大误差时像 MAE（梯度不会随误差线性增长而无限变大）

### 4.2 `nn.CrossEntropyLoss` —— 多分类

CrossEntropyLoss 是分类任务中无可争议的王者。几乎所有的图像分类、文本分类任务都使用它。

最容易犯的错误是**在模型最后一层加了 Softmax 再用 CrossEntropyLoss**。CrossEntropyLoss 的输入必须是 **logits（未经过 softmax 的原始输出）**，标签必须是**类别索引（整数，不是 one-hot）**。这是因为 PyTorch 的 CrossEntropyLoss 内部已经包含了 `LogSoftmax` + `NLLLoss` 的组合——如果你事先加了 Softmax，就相当于做了两次指数归一化，结果当然不对。

```python
criterion = nn.CrossEntropyLoss()

# 输入：logits（未经过 softmax 的原始输出），shape (batch, num_classes)
# 标签：类别索引，shape (batch,) — 不是 one-hot!
logits = torch.randn(4, 10)    # 4 个样本，10 分类
labels = torch.tensor([2, 5, 1, 9])  # 真实类别

loss = criterion(logits, labels)
print(loss)
```

> CrossEntropyLoss 内部已经包含 `LogSoftmax` + `NLLLoss`。**不要在模型最后一层加 Softmax**，直接输出 logits 即可。

**CrossEntropyLoss 内部拆解**：

理解 CrossEntropyLoss 的内部实现能帮助你避免一些常见的误解。以下是手动等价实现的验证：

```python
# CrossEntropyLoss 等价于 LogSoftmax + NLLLoss
logits = torch.randn(4, 10)
labels = torch.tensor([2, 5, 1, 9])

# 方式 1: 直接用 CrossEntropyLoss
loss1 = nn.CrossEntropyLoss()(logits, labels)

# 方式 2: 手动等价实现
log_probs = nn.LogSoftmax(dim=1)(logits)  # log(softmax(logits))
loss2 = nn.NLLLoss()(log_probs, labels)   # 取标签对应位置的负对数概率

print(f"CrossEntropyLoss: {loss1:.4f}")
print(f"LogSoftmax+NLL:   {loss2:.4f}")
print(f"一致: {torch.allclose(loss1, loss2)}")  # True

# 手算验证（对第一个样本）
logit_sample = logits[0]
# Softmax: p_i = exp(logit_i) / sum_j(exp(logit_j))
probs = torch.softmax(logit_sample, dim=0)
# CrossEntropy: -log(p_label)
label = labels[0].item()
manual_loss = -torch.log(probs[label])
print(f"手动计算 loss: {manual_loss:.4f}")
print(f"NLLLoss 第一样本: {log_probs[0, label]:.4f}")
```

这个等价性说明了 CrossEntropyLoss 在数值上做得很聪明：它用 log-sum-exp 技巧直接在 logits 上计算，避免了先做 softmax（可能导致 exp 溢出）再做 log（可能得到 -inf）的数值不稳定问题。

**类别不平衡的 `weight` 参数**：

```python
# 假设类别 2 样本很少，给更大权重
class_weights = torch.tensor([1.0, 1.0, 5.0])  # 增大类别 2 的权重
criterion = nn.CrossEntropyLoss(weight=class_weights)

logits = torch.randn(4, 3)
labels = torch.tensor([0, 1, 2, 2])

loss = criterion(logits, labels)
# 类别 2 的预测错误会受到更大的惩罚
```

`weight` 参数的工作原理是：在计算 loss 时，每个样本的 loss 会乘以该类别的权重。当某个类别样本很少时，增大其权重可以平衡各类别对总 loss 的贡献，防止模型偏向多数类。

### 4.3 `nn.BCEWithLogitsLoss` —— 二分类 / 多标签分类

对于二分类（一个样本只属于一个类别）和多标签分类（一个样本可以同时属于多个类别），BCEWithLogitsLoss 是推荐的选择。

```python
criterion = nn.BCEWithLogitsLoss()

# 二分类
logits = torch.randn(8)              # 8 个样本的 logits（标量）
labels = torch.randint(0, 2, (8,)).float()  # 0 或 1
loss = criterion(logits, labels)

# 多标签分类（每个样本可同时属于多个类别）
logits = torch.randn(8, 5)           # 8 个样本，5 个标签
labels = torch.randint(0, 2, (8, 5)).float()  # 每个标签独立
loss = criterion(logits, labels)
```

> 二分类场景优先使用 `BCEWithLogitsLoss` 而非 `BCELoss` + Sigmoid。前者使用 log-sum-exp 技巧，数值更稳定。

**BCEWithLogitsLoss vs BCELoss 数值稳定性对比**：

为什么 BCEWithLogitsLoss 比 BCELoss + Sigmoid 更稳定？关键在于 log(0) 和 log(1) 的数值问题。当 logit 非常大（如 100）时，sigmoid 的值接近于 1，1 - sigmoid 接近于 0，log(1-0) 就是 log(0) = -inf。BCEWithLogitsLoss 通过数学变换避免了这个问题。

```python
# 极端 logit 值下的数值稳定性
extreme_logits = torch.tensor([-100.0, 100.0])
labels = torch.tensor([0.0, 1.0])

# BCEWithLogitsLoss: 数值稳定
loss_stable = nn.BCEWithLogitsLoss()(extreme_logits, labels)
print(f"BCEWithLogitsLoss: {loss_stable:.4f}")  # 50.0

# BCELoss + Sigmoid: log(0) 和 log(1) 导致数值问题
probs = torch.sigmoid(extreme_logits)  # [~0, ~1]
loss_unstable = nn.BCELoss()(probs, labels)
print(f"BCELoss: {loss_unstable:.4f}")
# -log(0) ≈ inf, log(1-1) = log(0) ≈ -inf
# BCELoss 内部有 eps 保护，但 BCEWithLogitsLoss 更稳健
```

### 损失函数速查表

| 任务 | 损失函数 | 模型最后一层 | 标签格式 |
|------|---------|-------------|---------|
| 回归 | MSELoss | 无激活 | 连续值 float32 |
| 回归（抗异常值） | L1Loss / SmoothL1Loss | 无激活 | 连续值 float32 |
| 二分类 | BCEWithLogitsLoss | 无激活（1 个 logit） | 0/1 float32 |
| 多分类 | CrossEntropyLoss | 无激活（N 个 logits） | 类别索引 int64 |
| 多标签 | BCEWithLogitsLoss | 无激活（N 个 logits） | 0/1 float32 (多维) |
| 排序/度量学习 | TripletMarginLoss | 特征向量 | 三元组 (anchor, pos, neg) |
| KL 散度 | KLDivLoss | LogSoftmax | 概率分布 |
| 对比学习 | CrossEntropyLoss (temperature scaled) | 特征向量 | 正样本对 |

---

## 5. 参数初始化

参数初始化是深度学习中一个容易被低估但至关重要的环节。参数的初始值对训练收敛速度、最终性能、甚至训练是否能够成功有决定性影响。一个好的初始化让各层的激活值保持合理的方差范围；一个坏的初始化可能导致训练完全无法进行。

### 5.0 为什么参数初始化如此重要 —— 一个故事

想象你在构建一个 50 层的全连接网络。如果所有权重都初始化为 0，会发生什么？所有神经元计算出的值完全相同，反向传播的梯度也完全相同，网络等效于只有一层——因为所有神经元都在学习相同的模式。

如果权重初始化为过大的值（如 `N(0, 10)`），前向传播时激活值会逐层指数放大；反向传播时梯度也会逐层指数放大——这就是**梯度爆炸**，表现为 loss 变成 NaN 或 Inf。

如果权重初始化为过小的值（如 `N(0, 0.001)`），激活值会逐层缩小趋近于 0；sigmoid/tanh 进入饱和区，梯度接近 0——这就是**梯度消失**，表现为深层参数的梯度几乎为 0，训练停滞。

Xavier 和 Kaiming 初始化通过数学推导，将权重的方差精确控制在一个范围内，使得各层的输出方差保持一致。Xavier 初始化（Glorot 2010）针对 sigmoid/tanh 设计，Kaiming 初始化（He 2015）针对 ReLU 做了修正（因为 ReLU 丢弃了一半的信号，需要额外的方差补偿）。

PyTorch 默认使用 `kaiming_uniform_` 初始化 Linear 和 Conv2d 的权重，bias 初始化为 0。这意味着大多数情况下，默认初始化已经足够好。

### 5.1 常用初始化方法

```python
import torch.nn.init as init

linear = nn.Linear(100, 50)

# Xavier/Glorot 初始化
# 公式：W ~ U[-sqrt(6/(fan_in+fan_out)), sqrt(6/(fan_in+fan_out))]
# 适合 sigmoid/tanh 激活，保持前向和反向激活方差一致
init.xavier_uniform_(linear.weight)
init.xavier_normal_(linear.weight)
# normal 版本：W ~ N(0, 2/(fan_in + fan_out))

# Kaiming/He 初始化
# 公式：W ~ N(0, sqrt(2/fan_in))  (fan_in mode)
# 适合 ReLU 及其变体（因为 ReLU 丢掉了一半信号，需要额外方差）
init.kaiming_uniform_(linear.weight, nonlinearity='relu')
init.kaiming_normal_(linear.weight, nonlinearity='relu')
# mode='fan_in': 方差 = 2/fan_in (前向传播角度)
# mode='fan_out': 方差 = 2/fan_out (反向传播角度)

# 正交初始化（对 RNN/LSTM 有益，保持特征空间的正交性）
init.orthogonal_(linear.weight)

# 常量初始化
init.constant_(linear.weight, 0.0)
init.constant_(linear.bias, 0.01)

# 正态/均匀分布
init.normal_(linear.weight, mean=0.0, std=0.02)
init.uniform_(linear.weight, a=-0.1, b=0.1)

# 将 bias 置零（常见做法）
init.zeros_(linear.bias)
```

Kaiming 初始化的 `mode` 参数（`fan_in` vs `fan_out`）的选择取决于你的关注点：`fan_in` 保证前向传播时各层输出的方差一致；`fan_out` 保证反向传播时梯度的方差一致。对于 ReLU 网络，通常 `fan_in` 模式就够了。

### 5.2 各初始化方法的数学公式

| 方法 | 分布 | 参数 | 适用激活 |
|------|------|------|----------|
| Xavier Uniform | U[-a, a], a = sqrt(6/(fan_in+fan_out)) | 基于输入输出维度 | Sigmoid, Tanh |
| Xavier Normal | N(0, 2/(fan_in+fan_out)) | 基于输入输出维度 | Sigmoid, Tanh |
| Kaiming Uniform | U[-a, a], a = sqrt(6/fan_in) | fan_in (ReLU 修正) | ReLU, LeakyReLU |
| Kaiming Normal | N(0, 2/fan_in) | fan_in (ReLU 修正) | ReLU, LeakyReLU |
| Orthogonal | 正交矩阵 | - | RNN/LSTM |
| Lecun Normal | N(0, 1/fan_in) | fan_in | SELU |

### 5.3 自定义初始化函数

对于包含多种层的复杂网络，你需要一个初始化函数来处理不同层的不同初始化需求。

```python
def init_weights(m):
    """对模型所有层应用自定义初始化"""
    if isinstance(m, nn.Linear):
        init.xavier_uniform_(m.weight)
        if m.bias is not None:
            init.zeros_(m.bias)
    elif isinstance(m, nn.Conv2d):
        init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')
        if m.bias is not None:
            init.zeros_(m.bias)
    elif isinstance(m, nn.LSTM):
        for name, param in m.named_parameters():
            if 'weight' in name:
                init.orthogonal_(param)     # 正交初始化对 RNN 有益
            elif 'bias' in name:
                init.zeros_(param)
    elif isinstance(m, (nn.BatchNorm1d, nn.BatchNorm2d, nn.LayerNorm)):
        if m.weight is not None:
            init.ones_(m.weight)            # gamma 初始化为 1
        if m.bias is not None:
            init.zeros_(m.bias)             # beta 初始化为 0

model = MyModel()
model.apply(init_weights)   # 递归应用到所有子模块
```

`model.apply(fn)` 是一个非常强大的方法：它会递归遍历所有子模块，对每个模块调用 `fn(module)`。这意味着你可以在一个函数中统一处理 Conv2d、Linear、BatchNorm 等不同类型的层的初始化，不需要手动遍历 `model.children()`。

BatchNorm 的 gamma 初始化为 1、beta 初始化为 0 是标准做法——这意味着初始时 BatchNorm 就是一个恒等映射（不对标准化后的值做任何调整），随着训练推进再慢慢学习最优的缩放和平移。

### 5.4 为什么需要正确的初始化

不合理的初始化（如全零或过大/过小的随机值）会导致：
- **梯度消失**：权值太小，反向传播梯度逐层衰减，深层几乎不更新
- **梯度爆炸**：权值太大，反向传播梯度逐层放大，训练不稳定
- Xavier 和 Kaiming 初始化通过将权重的方差控制在 `1/fan_in` 量级，使各层输出的方差保持一致，缓解了上述问题。

**初始化不当的典型症状**：

```python
# 症状 1: 全零初始化 -> 所有神经元梯度相同 -> 等价于一个神经元
# 症状 2: 权值太大 (std=10) -> 激活值饱和 -> 梯度接近 0 -> 训练停滞
# 症状 3: 权值太小 (std=0.001) -> 深层输出接近 0 -> 梯度消失

# 验证初始化效果的简单方法
def check_activation_stats(model, input_size):
    """检查模型各层激活值的均值和标准差"""
    x = torch.randn(32, *input_size)
    for name, module in model.named_modules():
        if isinstance(module, (nn.Linear, nn.Conv2d)):
            x = module(x)
            print(f"{name:20s} | mean={x.mean():+.4f} | std={x.std():.4f} | "
                  f"min={x.min():+.4f} | max={x.max():+.4f}")
    # 理想的输出：std 应该在合理范围内，不会每层衰减或膨胀
```

这个 `check_activation_stats` 函数是一个实用的诊断工具。如果你发现 std 随着层数增加而指数衰减（如第 1 层 1.0，第 5 层 0.01，第 10 层 1e-6），说明梯度正在消失；如果 std 指数增长（第 1 层 1.0，第 5 层 100，第 10 层 10000），说明梯度正在爆炸。

---

## 6. 构建网络的标准写法

有了层、激活函数、归一化和初始化的知识，现在我们可以组合它们来构建完整的网络。以下展示从简单到复杂的三类网络结构。

### 6.1 多层感知机 (MLP)

MLP（Multi-Layer Perceptron）是最基础的神经网络结构。它由全连接层、激活函数、BatchNorm 和 Dropout 交替堆叠而成。以下实现展示了标准的层排列顺序：

```python
class MLP(nn.Module):
    """三层全连接网络，带 Dropout 和 BatchNorm"""
    def __init__(self, input_dim, hidden_dim, output_dim, dropout=0.2):
        super().__init__()
        self.fc1 = nn.Linear(input_dim, hidden_dim)
        self.bn1 = nn.BatchNorm1d(hidden_dim)
        self.relu = nn.ReLU()
        self.dropout = nn.Dropout(dropout)

        self.fc2 = nn.Linear(hidden_dim, hidden_dim)
        self.bn2 = nn.BatchNorm1d(hidden_dim)

        self.fc3 = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        x = self.fc1(x)
        x = self.bn1(x)
        x = self.relu(x)
        x = self.dropout(x)

        x = self.fc2(x)
        x = self.bn2(x)
        x = self.relu(x)
        x = self.dropout(x)

        x = self.fc3(x)         # 输出 logits，不加激活
        return x
```

注意几个设计惯例：
1. 最后一层 `fc3` **不加激活函数**——输出的是 logits，损失函数（CrossEntropyLoss）内部会处理 softmax
2. 最后一层**不加 Dropout**——你不想在训练时随机丢弃最终输出
3. `BatchNorm` 放在 `Linear` 之后、`ReLU` 之前——这是 Post-Norm 的标准顺序

**MLP 中 BatchNorm 的两种顺序（Pre-Norm vs Post-Norm）**：

```python
# Post-Norm (传统): Linear -> BatchNorm -> Activation -> Dropout
# 是目前最常用的顺序

# Pre-Norm: BatchNorm -> Linear -> Activation -> Dropout
# 在某些情况下训练更稳定

# 选择：一般使用 Post-Norm；如果训练不稳定，尝试 Pre-Norm
```

### 6.2 简单 CNN

CNN 的结构遵循一个经典范式：**卷积层提取特征 + 池化层降低空间分辨率 + 展平 + 全连接层分类**。

```python
class SimpleCNN(nn.Module):
    """两个卷积层 + 两个全连接层的 CNN"""
    def __init__(self, num_classes=10):
        super().__init__()
        # 特征提取：卷积层
        self.conv1 = nn.Conv2d(1, 32, kernel_size=3, padding=1)
        self.conv2 = nn.Conv2d(32, 64, kernel_size=3, padding=1)
        self.pool = nn.MaxPool2d(2, 2)          # 2x2 池化，尺寸减半
        self.relu = nn.ReLU()

        # 分类头：全连接层
        self.fc1 = nn.Linear(64 * 7 * 7, 128)   # 28 -> 14 -> 7 (2次池化)
        self.fc2 = nn.Linear(128, num_classes)
        self.dropout = nn.Dropout(0.25)

    def forward(self, x):
        # 输入: (batch, 1, 28, 28)
        x = self.pool(self.relu(self.conv1(x)))  # (batch, 32, 14, 14)
        x = self.pool(self.relu(self.conv2(x)))  # (batch, 64, 7, 7)
        x = x.view(x.size(0), -1)                # 展平: (batch, 64*7*7)
        x = self.dropout(self.relu(self.fc1(x))) # (batch, 128)
        x = self.fc2(x)                          # (batch, num_classes)
        return x
```

`x.view(x.size(0), -1)` 是做展平的标准写法。`x.size(0)` 获取 batch size，`-1` 让 PyTorch 自动计算展平后的维度。永远不要硬编码 batch_size（如 `x.view(32, -1)`），这会导致 batch_size 变化时出错。

**更深入的 CNN 构建模式**：

当网络变复杂时，将重复的层模式封装为可复用的模块（如 `CNNBlock`）是好的工程实践。

```python
class CNNBlock(nn.Module):
    """可复用的 CNN 模块：Conv -> BN -> ReLU -> Pool"""
    def __init__(self, in_c, out_c, kernel=3, pool=True):
        super().__init__()
        self.conv = nn.Conv2d(in_c, out_c, kernel, padding=kernel//2)
        self.bn = nn.BatchNorm2d(out_c)
        self.relu = nn.ReLU(inplace=True)
        self.pool = nn.MaxPool2d(2) if pool else nn.Identity()

    def forward(self, x):
        return self.pool(self.relu(self.bn(self.conv(x))))


class ModularCNN(nn.Module):
    """模块化 CNN，易于修改深度"""
    def __init__(self, in_channels, num_classes, channel_list=[32, 64, 128]):
        super().__init__()
        self.blocks = nn.ModuleList()
        prev_c = in_channels
        for c in channel_list:
            self.blocks.append(CNNBlock(prev_c, c))
            prev_c = c

        # 计算展平后大小（假设输入 32x32，每层池化减半）
        final_size = 32 // (2 ** len(channel_list))
        self.fc = nn.Linear(channel_list[-1] * final_size * final_size,
                             num_classes)

    def forward(self, x):
        for block in self.blocks:
            x = block(x)
        x = x.view(x.size(0), -1)
        return self.fc(x)
```

模块化设计让你可以通过修改 `channel_list` 来轻松改变网络的深度和宽度——`[32, 64, 128]` 是三层，`[32, 64, 128, 256]` 是四层，不需要修改其他任何代码。

### 6.3 `nn.Sequential` —— 简单网络的快捷构建

对于没有分支结构的纯前馈网络，`nn.Sequential` 提供了一种极简的写法。

```python
# 对于纯顺序的网络，用 Sequential 更简洁
model = nn.Sequential(
    nn.Linear(784, 256),
    nn.ReLU(),
    nn.Dropout(0.2),
    nn.Linear(256, 128),
    nn.ReLU(),
    nn.Dropout(0.2),
    nn.Linear(128, 10),
)

x = torch.randn(32, 784)
print(model(x).shape)  # torch.Size([32, 10])
```

`nn.Sequential` 的局限性：无法表达分支结构（如残差连接、多输入/输出）。对于复杂网络，仍需继承 `nn.Module`。基本上，如果你发现自己在思考"这个能不能用 Sequential"，答案很可能是"不够灵活，用 nn.Module"。

**Sequential 的高级用法 —— OrderedDict 命名层**：

```python
from collections import OrderedDict

model = nn.Sequential(OrderedDict([
    ("fc1", nn.Linear(784, 256)),
    ("relu1", nn.ReLU()),
    ("dropout1", nn.Dropout(0.2)),
    ("fc2", nn.Linear(256, 128)),
    ("relu2", nn.ReLU()),
    ("dropout2", nn.Dropout(0.2)),
    ("fc3", nn.Linear(128, 10)),
]))

# 可以按名称访问子模块
print(model.fc1)        # Linear(784, 256)
print(model[0])         # 也可以按索引访问
```

**动态构建 Sequential**：

```python
layers = []
layer_sizes = [784, 512, 256, 128, 10]
for i in range(len(layer_sizes) - 1):
    layers.append(nn.Linear(layer_sizes[i], layer_sizes[i+1]))
    if i < len(layer_sizes) - 2:  # 最后一层不加激活和 Dropout
        layers.append(nn.ReLU())
        layers.append(nn.Dropout(0.3))

model = nn.Sequential(*layers)
print(model)
```

### 6.4 带残差连接的网络

残差连接（Residual Connection）是深度学习史上最重要的架构创新之一。在残差网络出现之前，人们发现"网络越深效果越差"——不是因为过拟合，而是因为梯度无法有效传递到浅层。残差连接通过一个简单的恒等式解决了这个问题：

```
y = F(x) + x
```

梯度流动为 `dy/dx = dF/dx + 1`。那个 `+1` 是神来之笔——即使 `dF/dx` 接近 0（梯度消失），梯度至少为 1，信息仍然能通过捷径（shortcut）反向传播。这使得训练 100+ 层的网络成为可能。

```python
class ResidualBlock(nn.Module):
    """残差块：y = F(x) + x"""
    def __init__(self, dim, dropout=0.1):
        super().__init__()
        self.fc1 = nn.Linear(dim, dim)
        self.fc2 = nn.Linear(dim, dim)
        self.relu = nn.ReLU()
        self.dropout = nn.Dropout(dropout)

    def forward(self, x):
        identity = x                               # 保存捷径输入
        out = self.fc1(x)
        out = self.relu(out)
        out = self.dropout(out)
        out = self.fc2(out)
        out = out + identity                       # 残差连接
        out = self.relu(out)
        return out

# 堆叠多个残差块
class ResMLP(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim, num_blocks=3):
        super().__init__()
        self.input_proj = nn.Linear(input_dim, hidden_dim)  # 维度对齐
        self.blocks = nn.Sequential(*[ResidualBlock(hidden_dim) for _ in range(num_blocks)])
        self.output_proj = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        x = self.input_proj(x)
        x = self.blocks(x)
        x = self.output_proj(x)
        return x
```

残差连接的核心思想：网络学习的是**残差** `F(x)` 而非直接的目标映射 `H(x)`，即 `H(x) = F(x) + x`。当 `F(x) = 0` 时，信息可以直接沿捷径传递（恒等映射），这使得训练极深网络成为可能——即使深层梯度接近 0，信息也能通过捷径反向传播。

**残差连接的梯度流动分析**：

```
y = F(x) + x
dy/dx = dF/dx + 1

即使 dF/dx 接近 0（梯度消失），+1 确保梯度至少为 1，
不会完全消失。这是残差网络能训练 100+ 层的根本原因。
```

**残差连接维度不匹配时的处理**：

当输入和输出的维度不同时，捷径不能直接做恒等映射——需要投影（1x1 卷积或 Linear）来对齐维度。

```python
class ResidualBlockWithProjection(nn.Module):
    """支持维度变化的残差块"""
    def __init__(self, in_dim, out_dim):
        super().__init__()
        self.fc1 = nn.Linear(in_dim, out_dim)
        self.fc2 = nn.Linear(out_dim, out_dim)
        self.relu = nn.ReLU()

        # 如果输入输出维度不同，需要做投影（1x1 conv 或 Linear）
        self.projection = None
        if in_dim != out_dim:
            self.projection = nn.Linear(in_dim, out_dim, bias=False)

    def forward(self, x):
        identity = x
        if self.projection is not None:
            identity = self.projection(x)  # 维度对齐

        out = self.relu(self.fc1(x))
        out = self.fc2(out)
        out = out + identity
        return self.relu(out)
```

投影层 `self.projection` 使用 `bias=False` 是一个常见做法——投影层的目的只是对齐维度，不需要额外的偏置项增加参数。

---

## 7. 查看模型结构

在训练前，确认模型的层结构和参数量是一个好习惯。它能帮你发现设计错误（如忘记连接某层、参数量异常大等）。

### 7.1 `print(model)`

```python
model = SimpleCNN(num_classes=10)
print(model)
# 输出：
# SimpleCNN(
#   (conv1): Conv2d(1, 32, kernel_size=(3, 3), stride=(1, 1), padding=(1, 1))
#   (conv2): Conv2d(32, 64, kernel_size=(3, 3), stride=(1, 1), padding=(1, 1))
#   (pool): MaxPool2d(kernel_size=2, stride=2, padding=0, dilation=1, ceil_mode=False)
#   ...
# )
```

### 7.2 参数统计

了解模型的参数量有两个实用价值：估算内存/显存占用（参数量的 4 倍是 fp32 存储），评估模型复杂度是否符合预期。

```python
def count_parameters(model):
    """统计模型的可训练参数总数"""
    total = sum(p.numel() for p in model.parameters() if p.requires_grad)
    return total

print(f"Total parameters: {count_parameters(model):,}")

# 逐层统计
for name, param in model.named_parameters():
    if param.requires_grad:
        print(f"{name:25s} {str(list(param.shape)):20s} {param.numel():>8,}")
```

**更详细的参数分析**：

```python
def analyze_model(model):
    """详细分析模型结构"""
    print(f"{'Layer':<25s} {'Output Shape':<20s} {'Param #':>10s}")
    print("-" * 60)

    total_params = 0
    total_trainable = 0

    for name, param in model.named_parameters():
        param_count = param.numel()
        total_params += param_count
        if param.requires_grad:
            total_trainable += param_count

        print(f"{name:<25s} {str(list(param.shape)):<20s} {param_count:>10,}")

    print("-" * 60)
    print(f"Total params:      {total_params:>10,}")
    print(f"Trainable params:  {total_trainable:>10,}")
    print(f"Non-trainable:     {total_params - total_trainable:>10,}")

    # 模型大小估算
    size_mb = total_params * 4 / (1024 * 1024)  # float32 = 4 bytes
    print(f"Model size (fp32): {size_mb:.2f} MB")

analyze_model(SimpleCNN())
```

### 7.3 `torchsummary`（需安装 `pip install torchsummary`）

```python
from torchsummary import summary

model = SimpleCNN(num_classes=10)
summary(model, input_size=(1, 28, 28))
# 输出逐层的输出 shape 和参数量
```

### 7.4 torchinfo（`torchsummary` 的现代替代品）

torchinfo 是 torchsummary 的继任者，提供更详细的分析（包括估算的乘加操作数 Mult-Adds，这对评估模型计算量很有用）。

```python
# pip install torchinfo
from torchinfo import summary

# 更详细：显示 FLOPs、内存占用等
summary(model, input_size=(1, 1, 28, 28),
        col_names=["input_size", "output_size", "num_params",
                    "kernel_size", "mult_adds"])
```

---

## 8. Hooks：调试与特征提取

Hooks 是 PyTorch 提供的一种强大的"侧面通道"机制。它们允许你在模型的前向和反向传播过程中插入自定义逻辑，而**不需要修改模型代码本身**。这对于调试、特征可视化和梯度监控来说非常实用。

Hooks 的核心价值在于：它把"观察模型内部状态"从"修改模型代码"中解耦出来。你可以注册一个 hook 来监控某层的激活值分布，观察几个 epoch 后取消注册——模型代码完全不受影响。

### 8.1 Forward Hook

Forward Hook 在模块的 forward 方法执行**之后**被调用。它接收模块的输入和输出，最常用于提取中间层特征进行可视化或知识蒸馏。

```python
# Forward Hook: 在某个层的 forward 之后执行
activation_cache = {}

def save_activation_hook(name):
    """返回一个 hook 函数，将激活值保存到全局字典"""
    def hook(module, input, output):
        activation_cache[name] = output.detach()
        print(f"[Hook] {name}: input shape {input[0].shape} -> output shape {output.shape}")
    return hook

model = SimpleCNN()
# 注册 hook 到特定层
handle1 = model.conv1.register_forward_hook(save_activation_hook("conv1"))
handle2 = model.conv2.register_forward_hook(save_activation_hook("conv2"))

# 运行前向传播
x = torch.randn(1, 1, 28, 28)
output = model(x)

# 获取中间激活值
print(f"\nCached activations: {list(activation_cache.keys())}")
print(f"conv1 output shape: {activation_cache['conv1'].shape}")  # (1, 32, 14, 14)

# 使用完毕后移除 hook（避免内存泄漏）
handle1.remove()
handle2.remove()
```

`handle.remove()` 是一个重要的清理步骤。如果不移除 hook，每次 forward 都会触发 hook 函数并缓存激活值，导致内存泄漏。

### 8.2 Backward Hook

Backward Hook 在模块的梯度计算完成**之后**被调用。它接收模块的梯度输入和梯度输出，用于监控梯度流动、检测 NaN/Inf、或实现自定义的梯度修改。

```python
# Backward Hook: 在某个层的 backward 之后执行
grad_cache = {}

def save_gradient_hook(name):
    """返回一个 hook 函数，保存梯度信息"""
    def hook(module, grad_input, grad_output):
        # grad_input: 对模块输入的梯度（tuple）
        # grad_output: 对模块输出的梯度（tuple）
        if grad_output[0] is not None:
            grad_cache[name] = grad_output[0].detach()
            print(f"[Backward Hook] {name}: grad_output shape {grad_output[0].shape}, "
                  f"mean={grad_output[0].mean():.6f}, std={grad_output[0].std():.6f}")
    return hook

model = SimpleCNN()
handle = model.conv2.register_full_backward_hook(save_gradient_hook("conv2"))
# 注：register_backward_hook 已废弃，使用 register_full_backward_hook (PyTorch 1.9+)

# 运行前向+反向
x = torch.randn(1, 1, 28, 28)
output = model(x)
loss = output.sum()
loss.backward()
# [Backward Hook] conv2: grad_output shape (1, 64, 7, 7), mean=...

handle.remove()
```

### 8.3 Tensor Hook（监控梯度）

Tensor Hook 是更底层的 hook，直接挂载在 Tensor 上。当这个 Tensor 的梯度计算完成时（在 backward 过程中），hook 被调用。它特别适合用于检测梯度中的 NaN/Inf 值。

```python
def nan_check_hook(name):
    """检查梯度是否包含 NaN/Inf"""
    def hook(grad):
        if grad is not None:
            if torch.isnan(grad).any():
                print(f"[WARNING] NaN detected in {name}'s gradient!")
            if torch.isinf(grad).any():
                print(f"[WARNING] Inf detected in {name}'s gradient!")
            # 可以返回修改后的梯度（或 None 表示不修改）
        return grad
    return hook

# 对参数注册 hook
for name, param in model.named_parameters():
    if param.requires_grad:
        param.register_hook(nan_check_hook(name))
```

当训练过程中 loss 突然变成 NaN，你不知道是哪个参数的梯度出了问题。为所有参数注册 NaN 检测 hook 可以立刻定位到问题源头——哪个参数最先出现了 NaN 梯度。

### 8.4 Hooks 的实际应用场景

| 场景 | Hook 类型 | 用途 |
|------|-----------|------|
| 特征可视化 | Forward Hook | 提取 CNN 中间层的特征图并可视化 |
| 梯度监控 | Backward Hook / Tensor Hook | 检测梯度消失/爆炸 |
| 知识蒸馏 | Forward Hook | 提取教师网络中间层输出作为监督信号 |
| 模型剪枝 | Forward Hook | 评估各层的激活值分布，决定剪枝策略 |
| 调试 NaN | Tensor Hook | 精确定位哪个参数的梯度首先出现 NaN |
| Grad-CAM | Forward + Backward Hook | 计算类激活图，可视化模型决策依据 |

---

## 基础练习

### 练习 3-1: 搭建 MLP
构建一个三隐藏层的 MLP（input_dim=784, hidden_dim=[512, 256, 128], output_dim=10），包含 BatchNorm 和 Dropout。打印模型结构和总参数量。

### 练习 3-2: 搭建简单 CNN
构建一个 CNN：Conv(1,16,3) -> ReLU -> MaxPool(2) -> Conv(16,32,3) -> ReLU -> MaxPool(2) -> Linear(32*5*5, 10)。输入为 MNIST 风格 (1, 28, 28) 灰度图。验证各层输出 shape 是否正确。

### 练习 3-3: 损失函数实验
创建随机 logits 和标签，分别计算 MSELoss、CrossEntropyLoss、BCEWithLogitsLoss，理解它们的输入要求和输出数值范围。

### 练习 3-4: CrossEntropyLoss 内部拆解
手动实现 `LogSoftmax + NLLLoss` 验证与 `CrossEntropyLoss` 的等价性。比较带 `weight` 参数和不带的 loss 差异。

---

## 进阶练习

### 练习 3-5: 残差网络
实现一个带残差连接的 MLP（ResMLP），含 4 个残差块，每块由 Linear -> ReLU -> Linear 组成。对比相同深度的普通 MLP 在深度增加时的表现（用简单回归任务验证梯度流动）。

### 练习 3-6: 参数初始化对比
用三种不同初始化方法（零初始化、大随机初始化、Kaiming 初始化）初始化同一个 MLP，在一个简单数据集上训练 10 个 epoch，对比 loss 曲线，分析初始化对训练的影响。

### 练习 3-7: Forward Hook 特征提取
构建 CNN 模型，使用 forward hook 提取每层卷积输出的特征图，将第一层卷积的 32 个通道可视化（用 subplot 排列展示）。

### 练习 3-8: BatchNorm vs LayerNorm 实验
构建相同结构的 MLP，分别使用 BatchNorm1d 和 LayerNorm，在小 batch_size (2, 4, 8, 16, 32) 下对比训练稳定性和最终准确率。

---

## 常见错误

神经网络组件的使用中有许多"不会报错但效果不对"的陷阱。理解错误的底层原因比记忆正确的写法更重要。

### 错误 1: 忘记调用 `super().__init__()`

**错误原因**：`nn.Module` 的 `__init__` 内部做了很多关键的初始化工作——注册参数追踪字典、初始化 hooks 容器、设置 training 模式标志等。跳过 `super().__init__()` 意味着这些基础设施全都没有建立，后续的 `model.to("cuda")`、`model.parameters()` 等方法可能静默失效。

```python
# 错误
class MyModel(nn.Module):
    def __init__(self):
        # 缺少 super().__init__()
        self.fc = nn.Linear(10, 5)  # 不会报错

model = MyModel()
model.to("cuda")  # 参数可能不会正确注册到 GPU!

# 正确
class MyModel(nn.Module):
    def __init__(self):
        super().__init__()          # 必须调用！
        self.fc = nn.Linear(10, 5)
```

### 错误 2: CrossEntropyLoss 的标签用了 one-hot

**错误原因**：CrossEntropyLoss 期望标签是类别索引（整数），而不是 one-hot 向量。如果你传入了 one-hot，PyTorch 会报 shape 不匹配的 RuntimeError。这个错误发生的根本原因是很多人从其他框架（如 Keras）转来时，习惯了 one-hot 标签的格式。

```python
# 错误
criterion = nn.CrossEntropyLoss()
logits = torch.randn(4, 10)
labels = torch.eye(10)[[0, 1, 2, 3]]  # one-hot, shape (4, 10)
loss = criterion(logits, labels)       # RuntimeError！

# 正确
labels = torch.tensor([0, 1, 2, 3])    # 类别索引, shape (4,)
loss = criterion(logits, labels)
```

### 错误 3: 在模型末尾加了 Softmax + CrossEntropyLoss

**错误原因**：CrossEntropyLoss 内部已经包含了 LogSoftmax。如果你在模型最后手动加了 Softmax，相当于概率被"归一化了两次"——先变成概率分布（和为 1），再经过 CrossEntropyLoss 内部的 LogSoftmax（对概率再取 log）。这虽然不会报错，但数值会有偏差，导致模型难以训练或效果下降。

```python
# 错误
class BadModel(nn.Module):
    def forward(self, x):
        return nn.Softmax(dim=1)(self.fc(x))  # 多了一层 softmax

criterion = nn.CrossEntropyLoss()
# loss 数值可能不对，因为 CrossEntropyLoss 内部还会再做 softmax

# 正确
class GoodModel(nn.Module):
    def forward(self, x):
        return self.fc(x)  # 直接输出 logits，不加 softmax
```

### 错误 4: 忘记将模型移到 GPU

**错误原因**：模型参数和输入数据必须在同一个设备上。这是 PyTorch 最经典的错误之一。当你创建模型后忘记 `.to("cuda")`，而输入数据已经在 GPU 上，前向传播时会因为跨设备运算而报错。教训：创建模型后立即调用 `.to(device)`，并确保 DataLoader 出来的 batch 也统一 `.to(device)`。

```python
# 错误
model = MyModel()
x = torch.randn(32, 10).cuda()
y = model(x)   # RuntimeError: 参数在 CPU，输入在 GPU！

# 正确
model = MyModel().cuda()
x = torch.randn(32, 10).cuda()
y = model(x)
```

### 错误 5: `view` 前未计算正确的展平尺寸

**错误原因**：在 CNN 转换到全连接层时，需要做展平操作。如果硬编码展平后的尺寸，换一个输入尺寸模型就崩溃了。正确做法是使用 `x.size(0)` 获取动态 batch_size，`-1` 自动推断展平维度。

```python
# 错误：硬编码尺寸，换输入尺寸就崩溃
class CNN(nn.Module):
    def forward(self, x):
        x = self.conv(x)
        x = x.view(32, -1)   # 硬编码 batch_size!

# 错误做法：硬编码 batch_size
x = x.view(32, 64 * 7 * 7)

# 正确做法
x = x.view(x.size(0), -1)   # -1 自动推断展平后大小
```

### 错误 6: BatchNorm1d 输入维度不匹配

**错误原因**：`BatchNorm1d` 期望输入是 `(batch, features)` 或 `(batch, features, L)`——即 `num_features` 参数对应的维度是第 1 维（0-indexed）。如果输入是 `(batch, seq_len, features)`（NLP 常见的 3D 张量），特征维度在第 2 维，BatchNorm1d 会在错误的维度上做归一化。解决方案是 permute 维度，或改用 LayerNorm。

```python
# 错误
bn = nn.BatchNorm1d(128)
x = torch.randn(32, 50, 128)  # 3D tensor (batch, seq_len, features)
y = bn(x)  # 错误：BatchNorm1d 期望 (batch, features) 或 (batch, features, L)

# 正确：将 (batch, seq_len, features) reshape 为 (batch, features, seq_len)
x = x.permute(0, 2, 1)        # (32, 128, 50)
y = bn(x)                      # 现在 128 在 num_features 位置
y = y.permute(0, 2, 1)        # 再转回来 (32, 50, 128)
```

### 错误 7: 将不可训练的量定义为 `nn.Parameter`

**错误原因**：`nn.Parameter` 和 `register_buffer` 是两种不同的注册方式。Parameter 会被 optimizer 检测到并尝试更新（通过梯度下降），Buffer 只随模型保存/加载但不参与梯度计算。BatchNorm 的 `running_mean` 和 `running_var` 是 Buffer 的典型例子——它们需要随模型 checkpoint 一起保存，但绝对不能被 optimizer 更新。

```python
# 错误
class Model(nn.Module):
    def __init__(self):
        super().__init__()
        self.register_buffer("running_mean", torch.zeros(128))
        # 正确：buffer 不会被 optimizer 更新

        self.running_mean = nn.Parameter(torch.zeros(128))
        # 错误：Parameter 会被 optimizer 更新（除非显式设置 requires_grad=False）

# 正确区分：
# nn.Parameter:  需要梯度、需要被 optimizer 更新的量（如权重、偏置）
# register_buffer: 需要随模型保存/加载但不需要梯度的量（如 BatchNorm 的 running_mean）
```

### 错误 8: 使用普通 Python list 存储子模块

**错误原因**：普通的 Python list/dict 不会被 PyTorch 的模块追踪系统识别。`nn.Module` 内部通过 `__setattr__` 来拦截对属性的赋值，判断是否为 `nn.Module` 类型并注册。而 `self.layers = [nn.Linear(...), nn.Linear(...)]` 中，list 是属性，但 list 内部的 Linear 层只是 list 的元素——它们从未经过 `__setattr__` 的注册流程。

```python
# 错误
class Model(nn.Module):
    def __init__(self):
        super().__init__()
        self.layers = [nn.Linear(10, 20), nn.Linear(20, 5)]
        # 参数不会被注册！model.parameters() 返回空！

# 正确
class Model(nn.Module):
    def __init__(self):
        super().__init__()
        self.layers = nn.ModuleList([
            nn.Linear(10, 20),
            nn.Linear(20, 5)
        ])
        # 参数被正确注册
```

---

## 本章小结

本章构建了 PyTorch 神经网络编程的核心知识体系。从 nn.Module 的容器设计出发，我们学习了常用层、激活函数、损失函数、参数初始化和网络搭建的完整流程。以下是几个最重要的理解：

**关于 nn.Module 的设计哲学**：它用面向对象的方式管理神经网络的层次结构。`__init__` 声明组件，`forward` 定义数据流，Module 自动处理参数注册、设备迁移、模式切换等琐事。这种设计让你可以专注于网络架构本身，而不用操心底层的基础设施。

**关于层选择的心智模型**：Linear 做全连接变换（适合表格数据和分类头），Conv2d 做空间特征提取（适合图像），LSTM/GRU 做序列建模（适合时序数据）。每种层的参数形状和使用场景都不同——理解它们的输入输出 shape 是写对代码的前提。

**关于激活函数和损失函数的配对**：这是大多数错误的来源。隐藏层用 ReLU/GELU 引入非线性，输出层不加激活（或根据任务选用 sigmoid/softmax），损失函数期望 logits 而非概率。CrossEntropyLoss = LogSoftmax + NLLLoss；BCEWithLogitsLoss 比 BCELoss + Sigmoid 更稳定。

**关于初始化的故事**：坏的初始化导致梯度消失或爆炸，好的初始化（Xavier/Kaiming）通过精确控制方差来保持各层的信号尺度一致。初始化不当的症状——全零导致对称性、太大导致 NaN、太小导致不收敛——都应该能通过 `check_activation_stats` 来诊断。

**关于工程规范**：`nn.ModuleList` 而非 Python list、`x.view(x.size(0), -1)` 而非硬编码 batch_size、训练/推理的 transform 分离、`model.train()` / `model.eval()` 的调用——这些不是可选的"最佳实践"，而是避免隐蔽 bug 的必要纪律。

要点速查：

- `nn.Module` 是所有网络组件的基类，`__init__` 定义层，`forward` 定义数据流
- **Linear** `(in, out)` 做矩阵乘法，权重形状 `(out, in)`；**Conv2d** `(in_c, out_c, k, s, p)` 做空间卷积
- 1x1 卷积用于通道数升降维；两个 3x3 卷积的等效感受野等于一个 5x5 卷积
- LSTM 通过输入门、遗忘门、输出门控制信息流动，hidden state 是短期记忆，cell state 是长期记忆
- BatchNorm 沿 batch 维度归一化，适合 CNN/CV；LayerNorm 沿 feature 维度归一化，适合 Transformer/NLP
- ReLU 是 CNN/MLP 隐藏层首选，GELU 是 Transformer 标配；LeakyReLU 缓解 dying ReLU
- **CrossEntropyLoss 输入 logits**（不加 softmax），标签是类别索引（不是 one-hot）；BCEWithLogitsLoss 用于二分类/多标签
- CrossEntropyLoss 等价于 LogSoftmax + NLLLoss；BCEWithLogitsLoss 比 BCELoss+Sigmoid 数值更稳定
- Kaiming 初始化配合 ReLU，Xavier 初始化配合 Sigmoid/Tanh
- `nn.ModuleList` 和 `nn.ModuleDict` 用于动态管理子模块（不能用普通 Python list/dict）
- 参数绑定（Tied Weights）用于自编码器等对称结构
- 残差连接 `y = F(x) + x` 使深层网络训练成为可能，通过 `+1` 保证梯度不会完全消失
- `Sequential` 便捷但无分支能力；复杂模型用 `nn.Module` 子类
- Forward/Backward Hooks 是调试、特征提取和梯度监控的利器
