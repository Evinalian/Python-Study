# 第05章 向量数据库实战

## 学习目标

完成本章后，你将能够：

1. 理解 Embedding 模型的原理、维度和选型标准
2. 使用 OpenAI embedding API 和开源模型（BGE-M3、text2vec）生成向量
3. 掌握三种向量相似度计算方法和适用场景
4. 使用 ChromaDB 构建轻量级语义搜索系统
5. 了解 Milvus 的生产级部署和索引优化
6. 实现完整的语义搜索流程：文本 → 向量 → 存储 → 查询 → 检索 → 生成
7. 掌握过滤条件、元数据管理和批处理优化

## 前置知识

- Python 基础：类、函数、上下文管理器
- 向量概念：N 维空间中的点，用浮点数列表表示
- RAG 架构基础（第04章内容）
- 基本的 Docker 概念（Milvus 部署需要）
- pip 安装第三方库

---

## 1. Embedding 模型

### 1.1 什么是 Embedding

Embedding（向量嵌入）是将文本、图像等非结构化数据转换为一组固定长度的浮点数向量。这个向量在语义空间中代表了输入的含义。

```
"猫"      → [0.12, -0.34, 0.56, ...]  (1536维)
"小猫"    → [0.13, -0.32, 0.54, ...]  (与"猫"的向量很接近)
"汽车"    → [-0.45, 0.78, -0.23, ...]  (与"猫"的向量很远)
```

### 1.2 OpenAI Embedding 模型

```python
"""
OpenAI Embedding API 使用指南。

模型对比:
- text-embedding-3-small: 1536维, $0.02/1M tokens, 性能好, 性价比最高
- text-embedding-3-large: 3072维, $0.13/1M tokens, 效果最好
- text-embedding-ada-002: 1536维, $0.10/1M tokens, 旧版(不推荐新项目)
"""
import os
import numpy as np
import time
from openai import OpenAI

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    base_url=os.getenv("OPENAI_BASE_URL", "https://api.openai.com/v1"),
)


class OpenAIEmbeddings:
    """
    封装 OpenAI Embedding API。
    """

    def __init__(self, model: str = "text-embedding-3-small"):
        self.model = model
        self.dimensions = {
            "text-embedding-3-small": 1536,
            "text-embedding-3-large": 3072,
            "text-embedding-ada-002": 1536,
        }

    def embed(self, texts: list[str]) -> list[list[float]]:
        """
        将文本列表转换为向量列表。

        参数:
            texts: 文本列表，每批最多 2048 条

        返回:
            [[0.1, -0.2, ...], [0.3, 0.4, ...]] 维度 = len(texts) x dim
        """
        # OpenAI 建议每批不超过 2048 条
        batch_size = 100
        all_embeddings = []

        for i in range(0, len(texts), batch_size):
            batch = texts[i : i + batch_size]

            response = client.embeddings.create(
                model=self.model,
                input=batch,
                # text-embedding-3-small/large 支持指定输出维度
                # dimensions=512,  # 可选: 降低维度以减少存储和加速检索
            )

            embeddings = [item.embedding for item in response.data]
            all_embeddings.extend(embeddings)

            print(f"  批次 {i // batch_size + 1}: {len(batch)} 条文本完成")

        return all_embeddings

    def embed_single(self, text: str) -> list[float]:
        """将单条文本转换为向量"""
        return self.embed([text])[0]

    def similarity(self, a: list[float], b: list[float]) -> float:
        """计算两个向量的余弦相似度"""
        a_arr = np.array(a)
        b_arr = np.array(b)
        return np.dot(a_arr, b_arr) / (np.linalg.norm(a_arr) * np.linalg.norm(b_arr))


def demo_openai_embeddings():
    """演示 OpenAI Embedding 的基本用法"""

    model = OpenAIEmbeddings("text-embedding-3-small")

    # 对比语义相近和不相近的文本
    texts = [
        "Python是一种编程语言",
        "Java也是一种编程语言",
        "今天天气很好适合出去散步",
        "编程语言包括Python、Java、C++等",
    ]

    print("=== OpenAI Embedding 演示 ===\n")
    print(f"模型: {model.model}, 维度: {model.dimensions[model.model]}")

    t0 = time.time()
    embeddings = model.embed(texts)
    elapsed = time.time() - t0

    print(f"耗时: {elapsed:.2f}s")

    # 计算两两相似度
    print("\n--- 相似度矩阵 ---")
    for i, text_i in enumerate(texts):
        for j, text_j in enumerate(texts):
            if i < j:
                sim = model.similarity(embeddings[i], embeddings[j])
                print(f"  \"{text_i[:20]}...\" vs \"{text_j[:20]}...\": {sim:.3f}")

    # 预期结果:
    # "Python是..." vs "Java也是..." : 高相似度（都是编程语言）
    # "Python是..." vs "今天天气..." : 低相似度（话题无关）


if __name__ == "__main__":
    # demo_openai_embeddings()
    print("(取消注释以运行 OpenAI Embedding 演示)")
```

### 1.3 开源 Embedding 模型

```python
"""
开源 Embedding 模型对比和使用。

模型选择指南:
- BGE-M3 (BAAI): 多语言(中英), 1024维, MTEB排名靠前, 多语言场景首选
- M3E (moka-ai): 中文优化, 768维, 轻量
- text2vec-large-chinese: 中文, 1024维, 效果好
- all-MiniLM-L6-v2: 英文, 384维, 非常轻量

安装: pip install sentence-transformers
"""
import time
import numpy as np


class SentenceTransformerEmbeddings:
    """
    基于 sentence-transformers 的开源 Embedding 模型封装。

    优势:
    - 本地运行，无需 API 调用
    - 无调用次数限制
    - 数据不出本地（隐私合规）
    - 可以 fine-tune

    劣势:
    - 需要 GPU 才能高效运行
    - 效果可能不如商业 API
    - 占用本地内存
    """

    def __init__(self, model_name: str = "BAAI/bge-small-zh-v1.5"):
        """
        初始化模型。

        推荐模型:
        - "BAAI/bge-small-zh-v1.5": 512维, 中文, 轻量
        - "BAAI/bge-base-zh-v1.5": 768维, 中文, 均衡
        - "BAAI/bge-large-zh-v1.5": 1024维, 中文, 效果好
        - "BAAI/bge-m3": 1024维, 多语言
        - "moka-ai/m3e-base": 768维, 中文
        - "shibing624/text2vec-base-chinese": 768维, 中文
        """
        try:
            from sentence_transformers import SentenceTransformer
        except ImportError:
            raise ImportError("请安装: pip install sentence-transformers")

        print(f"加载模型: {model_name} ...")
        t0 = time.time()
        self.model = SentenceTransformer(model_name)
        self.model_name = model_name
        print(f"加载完成 ({time.time() - t0:.1f}s)")

    def embed(self, texts: list[str], batch_size: int = 32) -> list[list[float]]:
        """
        将文本列表转换为向量列表。

        参数:
            texts: 文本列表
            batch_size: 批处理大小（GPU 内存允许下越大越快）

        返回:
            向量列表
        """
        # BGE 模型需要在 query 前加前缀
        # 注意: 这里是将所有文本作为文档编码，所以不需要 query prefix
        embeddings = self.model.encode(
            texts,
            batch_size=batch_size,
            show_progress_bar=True,
            normalize_embeddings=True,  # L2 归一化，方便直接用点积
        )
        return embeddings.tolist()

    def embed_query(self, query: str) -> list[float]:
        """
        对查询文本进行编码（BGE 模型需要加 query prefix）。

        BGE 模型的特殊要求:
        - 文档: 直接编码
        - 查询: 编码前加 "为这个句子生成表示以用于检索相关文章：" 前缀
        """
        try:
            # BGE 模型的 query 指令
            query_with_instruction = f"为这个句子生成表示以用于检索相关文章：{query}"
            embedding = self.model.encode(
                [query_with_instruction],
                normalize_embeddings=True,
            )
            return embedding[0].tolist()
        except Exception:
            # 非 BGE 模型可能不需要 prefix
            return self.embed([query])[0]

    @property
    def dimension(self) -> int:
        """返回向量维度"""
        return self.model.get_sentence_embedding_dimension()


# ============================================================
# 模型选择决策树
# ============================================================
def recommend_embedding_model(
    language: str = "chinese",
    budget: str = "free",
    scale: str = "small",
) -> str:
    """
    根据需求推荐 Embedding 模型。

    参数:
        language: 主要语言 "chinese" / "english" / "multilingual"
        budget: 预算 "free" / "api"
        scale: 规模 "small" / "medium" / "large"
    """
    recommendations = {
        ("chinese", "free", "small"): "BAAI/bge-small-zh-v1.5",
        ("chinese", "free", "medium"): "BAAI/bge-base-zh-v1.5",
        ("chinese", "free", "large"): "BAAI/bge-large-zh-v1.5",
        ("english", "free", "small"): "sentence-transformers/all-MiniLM-L6-v2",
        ("english", "free", "medium"): "BAAI/bge-base-en-v1.5",
        ("multilingual", "free", "medium"): "BAAI/bge-m3",
        ("chinese", "api", "small"): "OpenAI text-embedding-3-small",
        ("chinese", "api", "large"): "OpenAI text-embedding-3-large",
        ("multilingual", "api", "medium"): "OpenAI text-embedding-3-small",
    }

    return recommendations.get(
        (language, budget, scale),
        "BAAI/bge-small-zh-v1.5",  # 默认推荐
    )


if __name__ == "__main__":
    print("=== 开源 Embedding 模型推荐 ===\n")
    print(f"中文/免费/小规模 → {recommend_embedding_model('chinese', 'free', 'small')}")
    print(f"多语言/免费/中等 → {recommend_embedding_model('multilingual', 'free', 'medium')}")
    print(f"中文/API/大规模 → {recommend_embedding_model('chinese', 'api', 'large')}")

    print("\n=== MTEB 排行榜参考 (2024) ===")
    print("  https://huggingface.co/spaces/mteb/leaderboard")
    print("  中文推荐: BGE-M3 > text2vec-large > M3E")
    print("  英文推荐: BGE-en > E5-mistral > GTE")
```

### 1.4 Embedding 价格和维度对比

```python
"""
Embedding 模型的价格和维度对比表。

选择标准:
1. 维度: 越低存储越小、检索越快，但精度可能下降
2. Max Tokens: 单次编码的最大 token 数
3. 多语言支持: 是否支持中英文混合
4. 价格: API 模型按 token 计费，开源模型免费但需要 GPU
"""

EMBEDDING_COMPARISON = """
┌────────────────────────────┬────────┬───────────┬──────────┬─────────┐
│ 模型                        │ 维度   │ Max Tokens│ 多语言   │ 价格/1M │
├────────────────────────────┼────────┼───────────┼──────────┼─────────┤
│ text-embedding-3-small     │ 1536   │ 8191      │ 是       │ $0.020  │
│ text-embedding-3-large     │ 3072   │ 8191      │ 是       │ $0.130  │
│ text-embedding-ada-002     │ 1536   │ 8191      │ 是       │ $0.100  │
│ BGE-M3 (开源)              │ 1024   │ 8192      │ 是       │ 免费     │
│ BGE-large-zh (开源)        │ 1024   │ 512       │ 中文     │ 免费     │
│ M3E-base (开源)            │ 768    │ 512       │ 中文     │ 免费     │
│ text2vec-base-chinese(开源)│ 768    │ 512       │ 中文     │ 免费     │
│ all-MiniLM-L6-v2 (开源)    │ 384    │ 256       │ 英文     │ 免费     │
└────────────────────────────┴────────┴───────────┴──────────┴─────────┘

关于维度:
- 1536维 × 4字节(float32) = 6KB/向量
- 100万个向量 = 6GB (仅向量数据，不含文本和索引)
- text-embedding-3-small 支持 dimensions 参数降维到 512 (精度微微下降)
"""

print(EMBEDDING_COMPARISON)
```

---

## 2. 向量相似度

### 2.1 三种相似度计算方法

```python
"""
三种向量相似度计算方法的详解和代码实现。

1. 余弦相似度 (Cosine Similarity): 测量方向相似度
2. 欧氏距离 (Euclidean Distance): 测量绝对距离
3. 内积 (Dot Product): 测量方向 + 长度

选择指南:
- 余弦相似度: 最常用，适合文本语义相似（忽略文本长度差异）
- 欧氏距离: 适合数值特征（如用户画像、商品属性）
- 内积: 当向量做了 L2 归一化后，等价于余弦相似度
"""
import numpy as np
import math


class VectorSimilarity:
    """向量相似度计算工具集"""

    @staticmethod
    def cosine(a: list[float], b: list[float]) -> float:
        """
        余弦相似度。

        公式: cos(θ) = (A·B) / (||A|| × ||B||)

        范围: [-1, 1]，越接近 1 越相似

        使用 Python 循环（便于理解）:
        dot = sum(ai * bi for ai, bi in zip(a, b))
        norm_a = math.sqrt(sum(ai * ai for ai in a))
        norm_b = math.sqrt(sum(bi * bi for bi in b))
        return dot / (norm_a * norm_b) if norm_a and norm_b else 0.0
        """
        a_arr = np.array(a)
        b_arr = np.array(b)
        dot = np.dot(a_arr, b_arr)
        norm_a = np.linalg.norm(a_arr)
        norm_b = np.linalg.norm(b_arr)

        if norm_a == 0 or norm_b == 0:
            return 0.0
        return float(dot / (norm_a * norm_b))

    @staticmethod
    def euclidean(a: list[float], b: list[float]) -> float:
        """
        欧氏距离。

        公式: d = sqrt(Σ(Ai - Bi)²)

        范围: [0, +∞)，越接近 0 越相似

        注意: 这是距离指标，值越小说明越相似。
        如果要转成相似度: similarity = 1 / (1 + distance)
        """
        a_arr = np.array(a)
        b_arr = np.array(b)
        distance = np.linalg.norm(a_arr - b_arr)
        return float(distance)

    @staticmethod
    def euclidean_similarity(a: list[float], b: list[float]) -> float:
        """将欧氏距离转为相似度 (0~1 之间)"""
        distance = VectorSimilarity.euclidean(a, b)
        return 1.0 / (1.0 + distance)

    @staticmethod
    def dot_product(a: list[float], b: list[float]) -> float:
        """
        内积。

        公式: A·B = Σ(Ai × Bi)

        范围: (-∞, +∞)，值越大越相似

        当向量已 L2 归一化后（||A|| = ||B|| = 1），内积 = 余弦相似度
        """
        return float(np.dot(np.array(a), np.array(b)))

    @staticmethod
    def compare_all(a: list[float], b: list[float]) -> dict:
        """一次性计算三种相似度"""
        return {
            "cosine": VectorSimilarity.cosine(a, b),
            "euclidean_distance": VectorSimilarity.euclidean(a, b),
            "euclidean_similarity": VectorSimilarity.euclidean_similarity(a, b),
            "dot_product": VectorSimilarity.dot_product(a, b),
        }


if __name__ == "__main__":
    # 演示: 归一化向量 vs 未归一化向量
    print("=== 向量相似度对比 ===\n")

    vec_a = [0.1, 0.2, 0.3, 0.4]
    vec_b = [0.15, 0.25, 0.35, 0.45]  # 与 a 方向相同，模长略大
    vec_c = [-0.1, -0.2, -0.3, -0.4]  # 与 a 方向相反

    for label, vec in [("相似向量", vec_b), ("相反向量", vec_c)]:
        result = VectorSimilarity.compare_all(vec_a, vec)
        print(f"vec_a vs {label}:")
        for key, value in result.items():
            print(f"  {key}: {value:.4f}")
        print()

    print("观察:")
    print("  - 余弦相似度: 关注方向，b与a方向相同→接近1")
    print("  - 欧氏距离: 关注绝对位置，c与a方向相反→距离大")
    print("  - 内积: 同时关注方向和长度")
```

---

## 3. ChromaDB 实战

### 3.1 安装与基础操作

```python
"""
ChromaDB 基础使用 —— 轻量级向量数据库，适合原型开发和学习。

安装: pip install chromadb

核心概念:
- Collection: 就是一张"表"，包含 documents, embeddings, metadatas, ids
- add: 插入数据
- query: 查询最相似的 N 个文档
- get: 按 ID 获取数据
- update/upsert/delete: 数据管理
"""
import chromadb
import os


def chroma_basic_demo():
    """
    ChromaDB 基础 CRUD 操作演示。
    """
    # 创建客户端
    # 方式1: 内存模式（数据不持久化，适合测试）
    # client = chromadb.Client()

    # 方式2: 持久化模式（数据保存到磁盘）
    persist_path = "./chroma_data"
    client = chromadb.PersistentClient(path=persist_path)

    # 创建或获取 Collection
    # 可以理解为 SQL 中的 CREATE TABLE IF NOT EXISTS
    collection = client.get_or_create_collection(
        name="my_documents",
        metadata={"description": "我的文档集合", "hnsw:space": "cosine"},  # 使用余弦相似度
    )

    # ---- 添加数据 ----
    # 数据由四部分组成:
    # - documents: 文本内容
    # - metadatas: 元数据（用于过滤）
    # - ids: 唯一标识（用于更新/删除）
    # - embeddings: 可选，如果不提供则 ChromaDB 自动生成

    collection.add(
        documents=[
            "Python是一种面向对象的编程语言",
            "JavaScript主要用于前端开发",
            "机器学习是人工智能的一个分支",
            "Django是一个Python Web框架",
            "React是Facebook开发的UI库",
        ],
        metadatas=[
            {"category": "编程语言", "difficulty": "初级"},
            {"category": "前端", "difficulty": "初级"},
            {"category": "AI", "difficulty": "中级"},
            {"category": "Web框架", "difficulty": "中级"},
            {"category": "前端", "difficulty": "中级"},
        ],
        ids=["doc_1", "doc_2", "doc_3", "doc_4", "doc_5"],
    )

    print(f"集合中有 {collection.count()} 条记录")

    # ---- 查询 ----
    # 方式1: 用文本查询（ChromaDB 自动生成 embedding）
    results = collection.query(
        query_texts=["Python Web开发"],
        n_results=3,
    )

    print("\n=== 查询: 'Python Web开发' ===")
    for i, (doc_id, doc, distance) in enumerate(
        zip(results["ids"][0], results["documents"][0], results["distances"][0])
    ):
        print(f"  {i + 1}. [{doc_id}] {doc} (距离: {distance:.4f})")

    # 方式2: 用向量查询（如果你已经有 embedding）
    # embedding = embedding_model.embed(["Python Web开发"])[0]
    # results = collection.query(query_embeddings=[embedding], n_results=3)

    # ---- 带元数据过滤的查询 ----
    results = collection.query(
        query_texts=["编程基础"],
        n_results=3,
        where={"difficulty": "初级"},  # 只搜索初级难度的文档
    )

    print("\n=== 查询: '编程基础' (仅初级) ===")
    for i, (doc_id, doc) in enumerate(
        zip(results["ids"][0], results["documents"][0])
    ):
        print(f"  {i + 1}. [{doc_id}] {doc}")

    # ---- 获取数据 ----
    doc = collection.get(ids=["doc_1"])
    print(f"\n=== 获取 doc_1 ===")
    print(f"  文档: {doc['documents'][0]}")
    print(f"  元数据: {doc['metadatas'][0]}")

    # ---- 更新数据 ----
    collection.update(
        ids=["doc_1"],
        metadatas=[{"category": "编程语言", "difficulty": "初级", "updated": True}],
    )

    # ---- 删除数据 ----
    # collection.delete(ids=["doc_5"])

    # ---- 获取集合信息 ----
    print(f"\n=== 集合信息 ===")
    print(f"  名称: {collection.name}")
    print(f"  记录数: {collection.count()}")
    print(f"  元数据: {collection.metadata}")


if __name__ == "__main__":
    print("=== ChromaDB 基础操作 ===\n")
    # chroma_basic_demo()
    print("(取消注释以运行)")
```

### 3.2 ChromaDB 的高级功能

```python
"""
ChromaDB 高级特性:
- 自定义 Embedding Function
- 复杂元数据过滤
- 批量操作
- Collection 管理
"""
import chromadb
from chromadb.utils import embedding_functions
import numpy as np


class ChromaDBAdvanced:
    """
    ChromaDB 高级用法封装。
    """

    def __init__(
        self,
        collection_name: str,
        persist_path: str = "./chroma_advanced",
        use_openai: bool = False,
        openai_api_key: str = None,
    ):
        self.client = chromadb.PersistentClient(path=persist_path)

        # 选择 Embedding 函数
        if use_openai and openai_api_key:
            # OpenAI Embedding
            self.embedding_fn = embedding_functions.OpenAIEmbeddingFunction(
                api_key=openai_api_key,
                model_name="text-embedding-3-small",
            )
            print("使用 OpenAI Embedding")
        else:
            # 默认: sentence-transformers (本地)
            self.embedding_fn = embedding_functions.SentenceTransformerEmbeddingFunction(
                model_name="all-MiniLM-L6-v2",
            )
            print("使用本地 Sentence-Transformer Embedding")

        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            embedding_function=self.embedding_fn,
            metadata={"hnsw:space": "cosine"},
        )

    def add_documents(self, documents: list[dict]):
        """
        批量添加文档。

        参数:
            documents: [{"text": "...", "metadata": {...}, "id": "..."}, ...]
        """
        ids = [doc.get("id", f"doc_{i}") for i, doc in enumerate(documents)]
        texts = [doc["text"] for doc in documents]
        metadatas = [doc.get("metadata", {}) for doc in documents]

        self.collection.add(
            documents=texts,
            metadatas=metadatas,
            ids=ids,
        )

        print(f"添加了 {len(documents)} 条文档，总计 {self.collection.count()} 条")

    def search(
        self,
        query: str,
        top_k: int = 5,
        where: dict = None,
        where_document: dict = None,
    ) -> list[dict]:
        """
        语义搜索。

        参数:
            query: 查询文本
            top_k: 返回数量
            where: 元数据过滤条件，如 {"category": "技术"}
            where_document: 文档内容过滤条件，如 {"$contains": "Python"}

        返回:
            [{"id": "...", "text": "...", "metadata": {...}, "score": 0.95}, ...]

        元数据过滤语法（where）:
        - 等于: {"field": "value"}
        - 不等于: {"field": {"$ne": "value"}}
        - 大于: {"field": {"$gt": 10}}
        - 范围: {"field": {"$gte": 5, "$lte": 100}}
        - 包含: {"field": {"$in": ["a", "b"]}}
        - AND组合: {"$and": [{"field1": "a"}, {"field2": "b"}]}
        - OR组合: {"$or": [{"field1": "a"}, {"field1": "b"}]}
        """
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k,
            where=where,
            where_document=where_document,
        )

        # 整理输出格式
        formatted = []
        for i in range(len(results["ids"][0])):
            formatted.append(
                {
                    "id": results["ids"][0][i],
                    "text": results["documents"][0][i],
                    "metadata": results["metadatas"][0][i] if results["metadatas"][0] else {},
                    "score": 1.0 - results["distances"][0][i] / 2.0,  # 距离转相似度(近似)
                }
            )

        return formatted

    def get_all(self) -> list[dict]:
        """获取所有文档"""
        results = self.collection.get()
        return [
            {
                "id": results["ids"][i],
                "text": results["documents"][i],
                "metadata": results["metadatas"][i],
            }
            for i in range(len(results["ids"]))
        ]

    def delete_collection(self):
        """删除整个集合"""
        self.client.delete_collection(self.collection.name)


# ============================================================
# 演示: 文档搜索引擎
# ============================================================
def demo_document_search():
    """
    构建一个简易文档搜索引擎。
    """
    search_engine = ChromaDBAdvanced(
        collection_name="tech_docs",
        persist_path="./chroma_tech",
        use_openai=False,  # 使用本地模型
    )

    # 添加文档
    docs = [
        {
            "id": "py_intro",
            "text": "Python是一种解释型、面向对象的高级编程语言。由Guido van Rossum于1991年创建。Python的设计哲学强调代码可读性。",
            "metadata": {"topic": "编程语言", "lang": "python", "level": "入门", "year": 2024},
        },
        {
            "id": "py_async",
            "text": "Python异步编程使用asyncio库。通过async/await关键字可以编写非阻塞的并发代码。事件循环是异步编程的核心。",
            "metadata": {"topic": "编程语言", "lang": "python", "level": "高级", "year": 2024},
        },
        {
            "id": "js_intro",
            "text": "JavaScript是一种脚本语言，主要用于Web前端开发。可以在浏览器中直接运行，操作DOM元素。",
            "metadata": {"topic": "前端", "lang": "javascript", "level": "入门", "year": 2023},
        },
        {
            "id": "ml_basic",
            "text": "机器学习是人工智能的分支。通过数据训练模型来进行预测。常用的Python库包括NumPy、Pandas和Scikit-learn。",
            "metadata": {"topic": "AI", "lang": "python", "level": "入门", "year": 2024},
        },
        {
            "id": "dl_intro",
            "text": "深度学习使用多层神经网络。PyTorch和TensorFlow是两个主流框架。GPU加速是深度学习的关键。",
            "metadata": {"topic": "AI", "lang": "python", "level": "中级", "year": 2024},
        },
        {
            "id": "web_django",
            "text": "Django是一个高级Python Web框架。它遵循MVC模式，提供了ORM、管理后台、表单处理等功能。",
            "metadata": {"topic": "Web开发", "lang": "python", "level": "中级", "year": 2023},
        },
    ]

    search_engine.add_documents(docs)

    # 测试查询
    test_queries = [
        ("Python入门基础", None),
        ("AI和机器学习", None),
        ("Python", {"level": "高级"}),  # 只要高级内容
        ("前端开发", None),
    ]

    print("\n" + "=" * 60)
    for query, filter_cond in test_queries:
        print(f"\n查询: '{query}'", end="")
        if filter_cond:
            print(f" (过滤: {filter_cond})", end="")
        print()

        results = search_engine.search(query, top_k=3, where=filter_cond)
        for i, r in enumerate(results):
            print(f"  {i + 1}. [{r['score']:.3f}] {r['text'][:60]}... ({r['metadata'].get('topic')})")


if __name__ == "__main__":
    # demo_document_search()
    print("(取消注释以运行 ChromaDB 文档搜索演示)")
```

---

## 4. Milvus 简介

### 4.1 Milvus 架构概览

```python
"""
Milvus —— 生产级向量数据库。

与 ChromaDB 的对比:
┌──────────────┬─────────────────┬──────────────────────┐
│    特性      │    ChromaDB     │       Milvus         │
├──────────────┼─────────────────┼──────────────────────┤
│ 定位         │ 轻量原型/学习   │ 生产级分布式          │
│ 安装         │ pip install     │ Docker Compose       │
│ 数据量级     │ ~百万向量       │ 数十亿向量            │
│ 分布式       │ 不支持          │ 支持(计算/存储分离)   │
│ 索引类型     │ HNSW            │ IVF_FLAT, HNSW, etc  │
│ 一致性       │ 最终一致        │ 可配置(强/最终)       │
│ 元数据过滤   │ 支持            │ 支持(Scalar Index)    │
│ 部署复杂度   │ 极低            │ 中-高                 │
│ 学习成本     │ 低              │ 中                    │
└──────────────┴─────────────────┴──────────────────────┘

安装 Milvus (Docker Compose):
  1. wget https://github.com/milvus-io/milvus/releases/download/v2.4.0/milvus-standalone-docker-compose.yml
  2. docker-compose up -d
  3. pip install pymilvus
"""

# Milvus 的核心概念:
MILVUS_CONCEPTS = """
Milvus 的数据模型:

Collection (集合)
  ├── Schema (模式定义)
  │   ├── id_field: 主键字段 (int64 / varchar)
  │   ├── vector_field: 向量字段 (FloatVector)
  │   └── scalar_fields: 标量字段 (int, float, varchar, bool)
  │
  ├── Index (索引)
  │   ├── IVF_FLAT: 倒排索引 + 精确搜索, 适合百万级
  │   ├── IVF_SQ8: 量化压缩, 减少内存
  │   ├── IVF_PQ: 乘积量化, 内存占用最小
  │   ├── HNSW: 图索引, 查询速度快, 适合千万级
  │   └── SCANN: 乘积量化 + 重新排序
  │
  └── Partition (分区)
      └── 按条件物理隔离数据, 提高查询效率

搜索参数:
  - metric_type: "L2" / "IP" / "COSINE"
  - nprobe: IVF 索引中搜索的聚类数 (越大越精确但越慢)
  - ef: HNSW 索引中搜索的范围 (越大越精确但越慢)
"""

print(MILVUS_CONCEPTS)
```

### 4.2 Milvus 基础操作示例

```python
"""
Milvus 基础操作 —— 连接、创建集合、插入、查询。

注意: 这个示例需要 Milvus 服务正在运行。
如果没有 Milvus 环境，阅读代码了解 API 即可。
"""
from pymilvus import (
    connections,
    Collection,
    CollectionSchema,
    FieldSchema,
    DataType,
    utility,
)

# Milvus 连接信息
MILVUS_HOST = "localhost"
MILVUS_PORT = "19530"
COLLECTION_NAME = "demo_collection"
VECTOR_DIM = 128  # 向量维度（需与 Embedding 模型匹配）


def connect_milvus():
    """连接到 Milvus 服务"""
    try:
        connections.connect(
            alias="default",
            host=MILVUS_HOST,
            port=MILVUS_PORT,
        )
        print(f"已连接到 Milvus ({MILVUS_HOST}:{MILVUS_PORT})")
        return True
    except Exception as e:
        print(f"连接失败: {e}")
        print("请确保 Milvus 服务正在运行:")
        print("  docker-compose up -d")
        return False


def create_collection():
    """
    创建 Milvus Collection。

    Schema 设计:
    - id: 主键 (VARCHAR, max_length=100)
    - text: 文本内容 (VARCHAR, max_length=5000)
    - embedding: 向量字段 (FLOAT_VECTOR, dim=VECTOR_DIM)
    - category: 分类标签 (VARCHAR, max_length=50)
    """
    # 定义字段
    fields = [
        FieldSchema(name="id", dtype=DataType.VARCHAR, is_primary=True, max_length=100),
        FieldSchema(name="text", dtype=DataType.VARCHAR, max_length=5000),
        FieldSchema(name="embedding", dtype=DataType.FLOAT_VECTOR, dim=VECTOR_DIM),
        FieldSchema(name="category", dtype=DataType.VARCHAR, max_length=50),
    ]

    # 定义 Schema
    schema = CollectionSchema(
        fields=fields,
        description="演示集合 - 文档搜索",
        enable_dynamic_field=True,  # 允许动态字段
    )

    # 创建 Collection
    if utility.has_collection(COLLECTION_NAME):
        utility.drop_collection(COLLECTION_NAME)
        print(f"已删除旧集合: {COLLECTION_NAME}")

    collection = Collection(name=COLLECTION_NAME, schema=schema)
    print(f"已创建集合: {COLLECTION_NAME}")
    return collection


def create_index(collection: Collection):
    """
    创建索引。

    索引类型选择:
    - IVF_FLAT: 数据量 < 100万时推荐
    - HNSW: 数据量 > 100万 或需要最快查询速度

    索引参数:
    - nlist: 聚类数 (IVF_FLAT)，建议 4*sqrt(N)
    - M: 每个节点的连接数 (HNSW)，建议 16-64
    - efConstruction: 构建时的搜索宽度 (HNSW)
    """
    index_params = {
        "metric_type": "COSINE",  # 余弦相似度
        "index_type": "IVF_FLAT",
        "params": {"nlist": 128},
    }

    collection.create_index(
        field_name="embedding",
        index_params=index_params,
    )
    print(f"索引创建完成: {index_params['index_type']} + {index_params['metric_type']}")


def insert_data(collection: Collection):
    """插入数据"""
    import numpy as np

    # 模拟数据
    entities = [
        ["doc_1", "doc_2", "doc_3", "doc_4"],  # ids
        [
            "Python是一种编程语言",
            "JavaScript用于前端开发",
            "机器学习是AI的分支",
            "Django是Python Web框架",
        ],  # texts
        np.random.rand(4, VECTOR_DIM).tolist(),  # embeddings (用随机向量模拟)
        ["language", "frontend", "ai", "framework"],  # categories
    ]

    collection.insert(entities)
    print(f"插入了 {len(entities[0])} 条数据")

    # 重要: 插入数据后需要 flush
    collection.flush()


def search_demo(collection: Collection):
    """
    向量搜索。

    搜索参数:
    - nprobe: 搜索的聚类数，越大越精确但越慢
    - limit: 返回结果数
    - expr: 标量过滤表达式 (类似 SQL WHERE)
    """
    import numpy as np

    # 加载集合到内存（搜索前必须执行）
    collection.load()

    # 模拟查询向量
    query_vector = np.random.rand(VECTOR_DIM).tolist()

    search_params = {"metric_type": "COSINE", "params": {"nprobe": 10}}

    # 基础搜索
    results = collection.search(
        data=[query_vector],
        anns_field="embedding",
        param=search_params,
        limit=3,
        output_fields=["text", "category"],  # 返回这些字段
    )

    print("\n=== 向量搜索结果 ===")
    for hits_i, hits in enumerate(results):
        print(f"查询 {hits_i + 1}:")
        for hit in hits:
            print(f"  id={hit.id}, distance={hit.distance:.4f}")
            print(f"    text={hit.entity.get('text')}")
            print(f"    category={hit.entity.get('category')}")

    # 带标量过滤的搜索
    results_filtered = collection.search(
        data=[query_vector],
        anns_field="embedding",
        param=search_params,
        limit=3,
        expr='category == "ai"',  # 只看 AI 类别
        output_fields=["text", "category"],
    )

    print("\n=== 过滤后搜索结果 (category=='ai') ===")
    for hits in results_filtered:
        for hit in hits:
            print(f"  id={hit.id}, category={hit.entity.get('category')}")


def milvus_workflow():
    """完整的 Milvus 工作流"""
    if not connect_milvus():
        print("跳过 Milvus 演示 (服务未运行)")
        return

    collection = create_collection()
    create_index(collection)
    insert_data(collection)

    # 创建索引后需要再次加载
    search_demo(collection)

    # 清理
    # utility.drop_collection(COLLECTION_NAME)


if __name__ == "__main__":
    print("=== Milvus 基础操作 ===\n")
    print("使用前请确保 Milvus 服务正在运行:")
    print("  docker-compose up -d")
    print()
    # milvus_workflow()
    print("(取消注释以运行，需要 Milvus 服务)")
```

### 4.3 Weaviate / Qdrant 简介

```python
"""
其他向量数据库简介。

Weaviate:
  - 特点: GraphQL API, 内置向量化和分类模块
  - 优势: 丰富的内置模块（NER、摘要、问答）
  - 缺点: 学习曲线较陡
  - 安装: docker run -p 8080:8080 semitechnologies/weaviate
  - Python: pip install weaviate-client

Qdrant:
  - 特点: Rust 编写，高性能，支持量化索引
  - 优势: 内存效率极高，支持 payload 索引
  - 缺点: 社区相对较小
  - 安装: docker run -p 6333:6333 qdrant/qdrant
  - Python: pip install qdrant-client
"""

VECTOR_DB_COMPARISON = """
┌──────────────┬──────────┬──────────┬───────────┬──────────┐
│    特性      │ ChromaDB │  Milvus  │  Weaviate │  Qdrant  │
├──────────────┼──────────┼──────────┼───────────┼──────────┤
│ 语言         │ Python   │ Go/C++   │ Go        │ Rust     │
│ 部署         │ pip      │ Docker   │ Docker    │ Docker   │
│ 分布式       │ ✗        │ ✓        │ ✓         │ ✓        │
│ 全文搜索     │ 基础     │ ✗        │ 内置      │ 内置     │
│ 量化压缩     │ ✗        │ ✓        │ ✓         │ ✓        │
│ 过滤能力     │ 基础     │ 强       │ 强        │ 强       │
│ 推荐阶段     │ 原型     │ 生产     │ 生产      │ 生产     │
│ 开源协议     │ Apache2  │ Apache2  │ BSD       │ Apache2  │
└──────────────┴──────────┴──────────┴───────────┴──────────┘

选择建议:
- 学习/原型 → ChromaDB (够用且最简单)
- 生产/大规模 → Milvus (生态最成熟)
- 需要内置NLP → Weaviate (模块丰富)
- 追求性能 → Qdrant (Rust实现，内存效率高)
"""

print(VECTOR_DB_COMPARISON)
```

---

## 5. 完整语义搜索实现

### 5.1 端到端流程

```python
"""
完整的语义搜索流程:
  文档 → 切块 → Embedding → 向量存储 → 查询向量化 → 检索 → 生成回答

这是第04章的 RAG 管道加上本章的向量数据库形成完整闭环。
"""
import os
import json
from openai import OpenAI
import chromadb

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    base_url=os.getenv("OPENAI_BASE_URL", "https://api.openai.com/v1"),
)


class SemanticSearchEngine:
    """
    完整的语义搜索引擎。

    集成: 文档加载 → Embedding → ChromaDB 存储 → 语义检索 → LLM 生成
    """

    def __init__(
        self,
        collection_name: str = "semantic_search",
        embedding_model: str = "text-embedding-3-small",
        generation_model: str = "gpt-4o",
        persist_path: str = "./semantic_search_db",
    ):
        self.embedding_model = embedding_model
        self.generation_model = generation_model

        # ChromaDB 客户端（通过 OpenAI embedding function）
        self.chroma_client = chromadb.PersistentClient(path=persist_path)

        # 使用 OpenAI Embedding
        from chromadb.utils import embedding_functions

        self.embedding_fn = embedding_functions.OpenAIEmbeddingFunction(
            api_key=os.getenv("OPENAI_API_KEY"),
            model_name=embedding_model,
        )

        self.collection = self.chroma_client.get_or_create_collection(
            name=collection_name,
            embedding_function=self.embedding_fn,
            metadata={"hnsw:space": "cosine"},
        )

    def index_texts(
        self,
        texts: list[str],
        metadatas: list[dict] = None,
        ids: list[str] = None,
    ):
        """
        索引文本数据。

        参数:
            texts: 文本列表
            metadatas: 元数据列表（可选）
            ids: ID 列表（可选）
        """
        if ids is None:
            ids = [f"doc_{i}" for i in range(len(texts))]

        self.collection.add(
            documents=texts,
            metadatas=metadatas if metadatas else [{}] * len(texts),
            ids=ids,
        )

        print(f"已索引 {len(texts)} 条文本，总计 {self.collection.count()} 条")

    def search(
        self,
        query: str,
        top_k: int = 5,
        where: dict = None,
    ) -> list[dict]:
        """
        语义搜索。

        返回: [{"id": "...", "text": "...", "metadata": {...}, "score": 0.95}, ...]
        """
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k,
            where=where,
        )

        formatted = []
        for i in range(len(results["ids"][0])):
            score = 1.0 - results["distances"][0][i] / 2.0  # 距离转相似度
            formatted.append(
                {
                    "id": results["ids"][0][i],
                    "text": results["documents"][0][i],
                    "metadata": results["metadatas"][0][i] if results["metadatas"][0] else {},
                    "score": round(score, 4),
                }
            )

        return formatted

    def ask(
        self,
        question: str,
        top_k: int = 5,
        where: dict = None,
    ) -> dict:
        """
        检索 + LLM 生成 = RAG 问答。

        返回: {"answer": "...", "sources": [...]}
        """
        # 1. 检索相关文档
        chunks = self.search(question, top_k=top_k, where=where)

        if not chunks:
            return {
                "answer": "抱歉，没有找到相关信息。",
                "sources": [],
            }

        # 2. 构建上下文
        context_parts = []
        for i, chunk in enumerate(chunks):
            source = chunk["metadata"].get("source", f"文档{i + 1}")
            context_parts.append(f"[来源: {source}]\n{chunk['text']}")

        context = "\n\n---\n\n".join(context_parts)

        # 3. 生成回答
        response = client.chat.completions.create(
            model=self.generation_model,
            messages=[
                {
                    "role": "system",
                    "content": "你是一个基于知识库的问答助手。只根据提供的参考资料回答问题。如果资料中没有答案，明确告知用户。标注引用来源。",
                },
                {
                    "role": "user",
                    "content": f"参考资料:\n{context}\n\n问题: {question}\n\n请根据以上资料回答。",
                },
            ],
            temperature=0.3,
        )

        answer = response.choices[0].message.content

        # 4. 提取来源
        sources = list(
            set(
                chunk["metadata"].get("source", chunk["id"])
                for chunk in chunks
            )
        )

        return {
            "answer": answer,
            "sources": sources,
            "retrieved_chunks": chunks,
        }

    def get_stats(self) -> dict:
        """获取搜索引擎的统计信息"""
        return {
            "total_documents": self.collection.count(),
            "collection_name": self.collection.name,
            "embedding_model": self.embedding_model,
            "generation_model": self.generation_model,
        }


def demo_semantic_search():
    """
    演示完整的语义搜索流程。
    """
    engine = SemanticSearchEngine(
        collection_name="company_docs",
        persist_path="./semantic_search_db",
    )

    # 模拟公司文档
    company_docs = [
        {
            "text": "公司年假政策: 入职满1年的员工享有5天年假，满3年享有10天，满5年享有15天。年假需提前一周申请。",
            "metadata": {"source": "员工手册", "category": "福利", "year": 2024},
        },
        {
            "text": "病假政策: 员工每年享有5天带薪病假。超过3天需要提供医院证明。病假申请表可在OA系统下载。",
            "metadata": {"source": "员工手册", "category": "福利", "year": 2024},
        },
        {
            "text": "加班政策: 工作日加班按1.5倍工资计算，周末加班按2倍，法定节假日按3倍。加班需提前审批。",
            "metadata": {"source": "员工手册", "category": "薪酬", "year": 2024},
        },
        {
            "text": "报销流程: 员工需在OA系统提交报销申请，上传发票照片。500元以下由部门经理审批，以上需总监审批。",
            "metadata": {"source": "财务制度", "category": "财务", "year": 2023},
        },
        {
            "text": "出差标准: 一线城市每日住宿补贴500元，其他城市350元。交通费实报实销。需要提前提交出差申请。",
            "metadata": {"source": "财务制度", "category": "出差", "year": 2024},
        },
        {
            "text": "培训政策: 公司每年提供2000元的培训预算，可用于在线课程、技术书籍和行业会议。需提供培训证明。",
            "metadata": {"source": "培训制度", "category": "发展", "year": 2024},
        },
    ]

    # 索引
    engine.index_texts(
        texts=[doc["text"] for doc in company_docs],
        metadatas=[doc["metadata"] for doc in company_docs],
    )

    # 测试问答
    questions = [
        "我有多少天年假？",
        "生病请假需要什么手续？",
        "公司报销流程是怎样的？",
        "公司有哪些假期政策？",
    ]

    print("\n" + "=" * 60)
    for q in questions:
        print(f"\nQ: {q}")
        result = engine.ask(q, top_k=3)
        print(f"A: {result['answer'][:200]}...")
        print(f"来源: {result['sources']}")


if __name__ == "__main__":
    # demo_semantic_search()
    print("(取消注释以运行完整语义搜索演示)")
```

---

## 6. 高级话题

### 6.1 增量更新与数据一致性

```python
"""
向量数据库的增量更新策略。

问题: 当源文档改变时，如何保证向量数据库中的内容是最新的？

策略:
1. 全量重建: 清空旧数据，重新索引（适合数据量小）
2. 增量更新: 只更新变化的文档（适合数据量大）
3. 版本标记: 每个文档带版本号，过期版本标记删除

ChromaDB 支持:
- collection.upsert(): 插入或更新（基于 ID 匹配）
- collection.update(): 更新指定 ID
- collection.delete(): 删除指定 ID
"""
import chromadb
from datetime import datetime


class IncrementalIndexer:
    """
    增量索引器 —— 只更新变化的文档。
    """

    def __init__(self, collection: chromadb.Collection):
        self.collection = collection
        self._doc_versions: dict[str, str] = {}  # {doc_id: content_hash}

    def _hash_content(self, content: str) -> str:
        """对内容做简单哈希"""
        import hashlib

        return hashlib.md5(content.encode()).hexdigest()

    def sync(self, documents: list[dict]) -> dict:
        """
        同步文档到向量数据库（增量更新）。

        参数:
            documents: [{"id": "...", "text": "...", "metadata": {...}}, ...]

        返回:
            {"added": n, "updated": n, "deleted": n, "unchanged": n}
        """
        stats = {"added": 0, "updated": 0, "deleted": 0, "unchanged": 0}

        # 当前文档 ID 集合
        current_ids = set()

        for doc in documents:
            doc_id = doc["id"]
            current_ids.add(doc_id)
            content_hash = self._hash_content(doc["text"])

            # 检查是否已存在且未变化
            if doc_id in self._doc_versions:
                if self._doc_versions[doc_id] == content_hash:
                    stats["unchanged"] += 1
                    continue
                else:
                    # 内容变了，更新
                    self.collection.update(
                        ids=[doc_id],
                        documents=[doc["text"]],
                        metadatas=[doc.get("metadata", {})],
                    )
                    stats["updated"] += 1
            else:
                # 新文档，添加
                self.collection.add(
                    ids=[doc_id],
                    documents=[doc["text"]],
                    metadatas=[doc.get("metadata", {})],
                )
                stats["added"] += 1

            # 记录版本
            self._doc_versions[doc_id] = content_hash

        # 删除已不存在的文档
        removed_ids = set(self._doc_versions.keys()) - current_ids
        if removed_ids:
            self.collection.delete(ids=list(removed_ids))
            stats["deleted"] = len(removed_ids)
            for rid in removed_ids:
                del self._doc_versions[rid]

        print(f"同步完成: 新增{stats['added']} 更新{stats['updated']} "
              f"删除{stats['deleted']} 不变{stats['unchanged']}")

        return stats


if __name__ == "__main__":
    print("=== 增量更新策略 ===\n")
    print("1. upsert: 根据 ID 自动判断插入或更新")
    print("2. 内容哈希: 对比内容是否变化，避免无效更新")
    print("3. 删除检测: 找出源中不存在的旧文档并清理")
```

### 6.2 性能优化

```python
"""
向量数据库性能优化指南。

优化维度:
1. 批量插入 → 降低网络往返开销
2. 索引类型选择 → 根据数据量和精度需求
3. 向量压缩 → 减少内存和存储
4. 分区策略 → 限制搜索范围
"""
import time
import numpy as np


def benchmark_batch_insert():
    """
    对比逐条插入 vs 批量插入的性能差异。

    ChromaDB 的 add() 方法已经是批量操作，内置优化。
    这个示例演示的是通用原则。
    """

    dim = 128
    batch_sizes = [10, 100, 500, 1000]
    total = 5000

    print("=== 批量插入性能对比 (模拟) ===\n")
    print(f"总数据量: {total} 条, 维度: {dim}")
    print()

    for batch_size in batch_sizes:
        # 模拟: 按批处理
        batches = total // batch_size
        t0 = time.time()

        for _ in range(batches):
            vectors = np.random.rand(batch_size, dim).tolist()
            # 模拟插入操作 (实际代码中这里调用 collection.add)
            np.array(vectors)  # 模拟处理时间

        elapsed = time.time() - t0
        print(f"batch_size={batch_size:>4}: {batches} 批, 耗时 {elapsed:.3f}s")
        print(f"    相当于 {total / elapsed:.0f} 条/秒")

    print("\n结论: batch_size 越大，吞吐量越高（但受内存限制）")


# ============================================================
# 索引类型选择指南
# ============================================================
INDEX_SELECTION_GUIDE = """
索引类型选择 (以 Milvus 为例):

数据量 < 100万:
  → HNSW (查询快) 或 IVF_FLAT (精度高)

数据量 100万 ~ 1000万:
  → IVF_SQ8 (内存友好) 或 HNSW (如果内存足够)

数据量 > 1000万:
  → IVF_PQ (内存最优) 或 DiskANN (磁盘索引)

索引参数调优:
  - nlist (IVF): 建议 4*sqrt(N), 例如 N=100万 → nlist=4000
  - nprobe (IVF 搜索): 建议 nlist/10, nprobe 越大精度越高但越慢
  - M (HNSW): 建议 16-64, 越大精度越高但内存越多
  - ef (HNSW 搜索): 建议 64-512, 越大精度越高但越慢
"""

print(INDEX_SELECTION_GUIDE)


if __name__ == "__main__":
    benchmark_batch_insert()
```

---

## 基础练习

### 练习 1: Embedding 模型对比
**场景**: 对比 OpenAI embedding 和开源模型的相似度计算结果。
**要求**: 准备测试文本对，用两种模型分别计算，分析差异。
**文件**: `exercise/ai-application/ch05_vector_db/ex1_embedding_compare.py`

### 练习 2: ChromaDB 文档管理
**场景**: 实现一个基于 ChromaDB 的文档存储和搜索系统。
**要求**: 支持增删改查、元数据过滤、持久化存储。
**文件**: `exercise/ai-application/ch05_vector_db/ex2_chromadb_docs.py`

### 练习 3: 语义搜索 vs 关键词搜索
**场景**: 对比语义搜索（embedding）和关键词搜索（BM25）的效果。
**要求**: 同一测试集，两种方式对比，分析各自的优势和劣势。
**文件**: `exercise/ai-application/ch05_vector_db/ex3_search_compare.py`

## 进阶练习

### 练习 4: 完整知识库问答
**场景**: 构建一个完整的知识库问答系统。
**要求**: 文档加载 → 切块 → embedding → ChromaDB → 检索 → LLM 生成 → 来源标注。
**文件**: `exercise/ai-application/ch05_vector_db/ex4_knowledge_qa.py`

### 练习 5: 混合搜索引擎
**场景**: 实现稠密 + 稀疏混合搜索引擎，用 ChromaDB 做稠密检索 + BM25 做稀疏检索。
**要求**: 加权融合两路结果，ReRank 精排，对比单一策略的改善。
**文件**: `exercise/ai-application/ch05_vector_db/ex5_hybrid_search.py`

---

## 常见错误

### 错误 1: 向量维度不匹配

```python
# 错误: Embedding 模型输出 1536 维，但数据库设置了 768 维
collection = client.create_collection(name="docs", dimension=768)
embeddings = embed_fn.embed(texts)  # 返回 1536 维向量
collection.add(embeddings=embeddings, ...)  # 报错: dimension mismatch

# 修正: 确保维度一致
dim = 1536  # 与 embedding 模型匹配
collection = client.create_collection(name="docs", dimension=dim)
```

### 错误 2: 忘记 normalize_embeddings 导致相似度不准

```python
# 错误: 使用内积作为相似度，但向量没有归一化
# 结果: 长文本的向量模长大，内积天然更大，不公平

# 修正: 编码时设置 normalize_embeddings=True
embeddings = model.encode(texts, normalize_embeddings=True)
# 此时内积 = 余弦相似度
```

### 错误 3: 批量操作时忽略 API 速率限制

```python
# 错误: 一次发送 10000 条文本到 OpenAI Embedding API
embeddings = client.embeddings.create(input=texts)  # 可能触发速率限制

# 修正: 分批发送，每批 100-500 条
batch_size = 100
for i in range(0, len(texts), batch_size):
    batch = texts[i : i + batch_size]
    embeddings = client.embeddings.create(input=batch)
    time.sleep(0.1)  # 避让速率限制
```

### 错误 4: ChromaDB 使用默认的 l2 距离而非 cosine

```python
# 错误: 使用默认的 l2 距离（欧氏距离）
collection = client.create_collection(name="docs")
# 对于归一化向量，欧氏距离和余弦相似度的排序相同，
# 但对未归一化的向量，欧氏距离可能偏好短文本

# 修正: 显式指定余弦相似度
collection = client.create_collection(
    name="docs",
    metadata={"hnsw:space": "cosine"},
)
```

### 错误 5: 查询向量和索引向量使用了不同的 embedding 模型

```python
# 错误: 索引时用 text-embedding-3-small, 查询时用 text-embedding-3-large
# 结果: 虽然都是语义向量，但分布在不同的向量空间中，相似度计算无意义

# 修正: 确保使用完全相同的 embedding 模型
# 在系统中统一管理 embedding model 实例
```

### 错误 6: 忘记关闭 Milvus 连接导致资源泄露

```python
# 错误: 脚本结束时没有断开连接
connections.connect(host="localhost", port="19530")
# ... 操作 ...
# 脚本结束，连接未正确释放

# 修正: 使用上下文管理器或显式断开
try:
    connections.connect(host="localhost", port="19530")
    # ... 操作 ...
finally:
    connections.disconnect("default")
```

---

## 本章小结

本章深入学习了向量数据库的实战体系：

| 知识点 | 核心要点 |
|--------|----------|
| Embedding模型 | OpenAI(text-embedding-3)、开源(BGE-M3/M3E/text2vec) |
| 向量维度 | 384~3072维，维度越高精度越好但存储和计算越慢 |
| 相似度计算 | 余弦相似度(最常用)、欧氏距离、内积 |
| ChromaDB | 轻量级，pip安装，适合原型和学习 |
| Milvus | 生产级，支持分布式和多种索引 |
| 语义搜索流程 | 文档→切块→embedding→存储→查询→检索→生成 |
| 性能优化 | 批量插入、索引调优、向量压缩 |
| 增量更新 | upsert + 内容哈希检测变化 |

至此，AI 应用开发五章教程全部结束。你已掌握从 Prompt Engineering 到 RAG 架构的完整技能体系：

```
第01章 (Prompt) → 第02章 (Function Calling) → 第03章 (Streaming)
       ↓                      ↓                        ↓
    控制模型行为           让模型执行操作         让输出更流畅
       
第04章 (RAG) → 第05章 (向量数据库)
       ↓              ↓
    外部知识检索      存储和检索基础设施
```

这些技能组合在一起，就是构建现代 AI 应用的完整技术栈。
