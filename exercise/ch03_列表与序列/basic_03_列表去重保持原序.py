"""
章节：第3章 列表与序列
题目：列表去重（保持原顺序）
类型：基础练习

题目描述：
写一个函数 dedup(lst)，移除列表中的重复元素，同时保持原始顺序。

示例输入/输出：
输入：  [1, 3, 2, 1, 5, 3, 2, 4]
输出：  [1, 3, 2, 5, 4]

提示：
- 使用 set 记录见过的元素（O(1) 成员检测）
- 遍历原列表，只把没见过的元素加入结果列表
- 不要直接用 set(lst) + list()，那样会丢失顺序
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 dedup(lst) 函数
# 2. 用 set 跟踪已见过的元素
# 3. 遍历列表，仅保留首次出现的元素
#
# 提示：
# - set 的成员检测是 O(1)
# - 不要用 set(list) 会丢失顺序
#
# 完成后，运行 python basic_03_列表去重保持原序.py 测试你的代码。


def dedup(lst):
    """去掉重复元素，保持原始顺序"""
    pass  # TODO: 实现此函数
