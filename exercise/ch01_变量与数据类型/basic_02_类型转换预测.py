"""
章节：第1章 认识 Python 与变量
题目：类型转换结果预测
类型：基础练习

题目描述：
写出下列表达式的值和类型，然后在 Python 中验证：

    a = int(3.14)
    b = float(10)
    c = bool("False")
    d = str(100) + str(200)
    e = True + 100

提示：
- int() 是截断取整，不是四舍五入
- bool() 把非空字符串转为 True（即使是 "False"）
- str(a) + str(b) 是字符串拼接
- True 在数值运算中等同于 1
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 执行上述 5 个表达式
# 2. 打印每个表达式的值和类型
# 3. 验证你的预测是否正确
#
# 提示：
# - 使用 type() 获取变量的类型
# - 注意 str + str 是字符串拼接而非数值加法
#
# 完成后，运行 python basic_02_类型转换预测.py 测试你的代码。
