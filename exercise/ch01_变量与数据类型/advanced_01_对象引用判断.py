"""
章节：第1章 认识 Python 与变量
题目：判断对象身份并解释可变/不可变行为
类型：进阶练习

题目描述：
写一个函数 same_object(a, b) 返回两个变量是否指向同一对象。然后用它来验证：

1. 对 int 类型：a = 5; b = 5 后 same_object(a, b) 是什么？
   a = a + 1 后，a 和原来的 5 还在同一个对象吗？

2. 对 list 类型：a = [1,2]; b = a 后 same_object(a, b) 是什么？
   a.append(3) 后，a 和 b 还指向同一对象吗？

提示：
- int 是不可变对象，"修改"意味着创建新对象
- list 是可变对象，append 是原地修改，不创建新对象
- 使用 id() 或 is 来判断是否同一个对象
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 same_object(a, b) 函数，用 is 判断两个变量是否指向同一对象
# 2. 分别测试 int（不可变）和 list（可变）两种类型的行为
# 3. 打印每次判断的结果并解释原因
#
# 提示：
# - int 是不可变对象，"修改"即创建新对象
# - list 是可变对象，append 原地修改，所有引用可见
#
# 完成后，运行 python advanced_01_对象引用判断.py 测试你的代码。


def same_object(a, b):
    """判断 a 和 b 是否指向同一个对象"""
    pass  # TODO: 实现此函数
