"""
章节：第4章 字典与集合
题目：统计字符出现次数
类型：基础练习

题目描述：
写一个函数 char_count(s)，用字典统计字符串中每个字符的出现次数（区分大小写）。

示例输入/输出：
输入：  "hello"
输出：  {'h': 1, 'e': 1, 'l': 2, 'o': 1}

提示：
- 遍历字符串的每个字符
- 使用 dict.get(ch, 0) 安全获取当前计数，然后 +1
- 或者用 collections.Counter 一行搞定
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 char_count(s) 函数
# 2. 遍历字符串，用字典记录每个字符的频次
# 3. 可尝试多种写法（手动判断 / get 默认值 / Counter）
#
# 提示：
# - counts[ch] = counts.get(ch, 0) + 1 是常用写法
#
# 完成后，运行 python basic_02_字符频次统计.py 测试你的代码。


def char_count(s):
    """统计每个字符出现次数"""
    pass  # TODO: 实现此函数
