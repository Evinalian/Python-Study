"""
章节：第1章 认识 Python 与变量
题目：预测 id 和 is 的输出
类型：基础练习

题目描述：
不运行代码，预测下列 print 的输出并解释原因：

    a = 10
    b = 10
    print(a is b)

    lst1 = [1, 2]
    lst2 = [1, 2]
    print(lst1 is lst2)
    print(lst1 == lst2)

    x = lst1
    print(x is lst1)

提示：
- is 比较两个变量是否指向同一个对象（身份比较）
- == 比较两个对象的值是否相等（值比较）
- CPython 会缓存 -5 到 256 之间的小整数
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 先写出每行 print 的预测结果和原因
# 2. 在代码中逐段验证你的预测
# 3. 使用 id() 辅助观察对象身份
#
# 提示：
# - is 比较身份，== 比较值
# - 小整数 (-5~256) 被 CPython 缓存
#
# 完成后，运行 python basic_03_id_is预测.py 测试你的代码。
