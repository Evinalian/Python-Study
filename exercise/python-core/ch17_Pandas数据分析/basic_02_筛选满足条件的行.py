"""
章节：第17章 Pandas 数据分析
题目：筛选满足条件的行
类型：基础练习

题目描述：
从以下 DataFrame 中筛选出 "技术" 部门且薪资大于 15000 的员工：

df = pd.DataFrame({
    "name": ["小王", "小李", "小张", "小陈", "小刘"],
    "department": ["技术", "销售", "技术", "销售", "技术"],
    "salary": [15000, 12000, 18000, 11000, 20000],
})

提示：
- 组合条件用 & (与)，每个条件用括号包起来
- 不能用 and 关键字（and 是 Python 关键字，不能被 Pandas 重载）
"""

import pandas as pd

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 创建题目中的 DataFrame
# 2. 用布尔索引筛选：df[(df["department"] == "技术") & (df["salary"] > 15000)]
# 3. 练习 query() 方法：df.query("department == '技术' and salary > 15000")
# 4. 练习其他筛选：isin()、~ 取反
#
# 提示：
# - 每个条件必须加括号，(条件1) & (条件2)
# - 参考第17章教程中条件筛选部分
#
# 完成后运行: python basic_02_筛选满足条件的行.py
