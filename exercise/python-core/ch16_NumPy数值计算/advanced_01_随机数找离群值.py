"""
章节：第16章 NumPy 数值计算
题目：生成随机数据并找出离群值（IQR 方法）
类型：进阶练习

题目描述：
生成 1000 个均值为 50、标准差为 10 的正态分布随机数。然后：
1. 计算第 5、25、50、75、95 百分位数
2. 用 IQR（四分位距）方法找出离群值：
   - 小于 Q1 - 1.5 * IQR 或大于 Q3 + 1.5 * IQR 的值
   - 其中 IQR = Q3 - Q1
3. 输出离群值的数量和具体值

提示：
- 使用 np.percentile(data, [5, 25, 50, 75, 95]) 计算分位数
- 离群值筛选使用布尔索引：data[(data < lower_bound) | (data > upper_bound)]
"""

import numpy as np

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 用 rng.normal(50, 10, 1000) 生成 1000 个正态分布随机数
# 2. 用 np.percentile() 计算分位数，Q1=25%, Q3=75%, IQR=Q3-Q1
# 3. 计算下界 = Q1 - 1.5*IQR，上界 = Q3 + 1.5*IQR
# 4. 用布尔索引筛选离群值，统计数量和比例
# 5. 打印数据分布概览（均值、标准差、偏度）
#
# 提示：
# - 参考第16章教程中 IQR 离群值检测部分
#
# 完成后运行: python advanced_01_随机数找离群值.py
