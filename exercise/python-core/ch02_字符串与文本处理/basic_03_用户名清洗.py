"""
章节：第2章 字符串与文本处理
题目：用户名清洗
类型：基础练习

题目描述：
写一个函数 clean_username(name)，它：
1. 去掉首尾空白
2. 把大写字母全部转成小写
3. 把中间连续的多个空白替换为单个空格

示例输入/输出：
输入：  "  张  三  "
输出：  "张 三"

输入：  "  Hello   World  "
输出：  "hello world"

提示：
- 使用 strip() 去掉首尾空白
- 使用 lower() 转小写
- 用 split() 按空白拆分（连续空白自动合并），再用 " ".join() 拼接
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 clean_username(name) 函数
# 2. 用给定的测试用例验证函数正确性
#
# 提示：
# - strip() → lower() → split() + join() 三步走
#
# 完成后，运行 python basic_03_用户名清洗.py 测试你的代码。


def clean_username(name):
    """清洗用户名：去空白、转小写、合并多余空格"""
    pass  # TODO: 实现此函数
