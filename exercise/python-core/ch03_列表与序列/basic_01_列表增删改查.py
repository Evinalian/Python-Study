"""
章节：第3章 列表与序列
题目：列表增删改查练习
类型：基础练习

题目描述：
给定初始列表 fruits = ["apple", "banana", "cherry"]，完成以下操作：

1. 在末尾添加 "orange"
2. 在 "banana" 后面插入 "grape"
3. 删除 "cherry"
4. 将 "apple" 修改为 "green apple"

提示：
- 末尾添加用 append()
- 中间插入用 insert(index, item)
- 按值删除用 remove(item)
- 按索引修改用 fruits[index] = new_value
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 定义初始列表 fruits
# 2. 依次执行增、插、删、改四个操作
# 3. 每步操作后打印列表，观察变化
#
# 提示：
# - append(item) 在末尾添加
# - insert(idx, item) 在指定位置插入
# - remove(item) 按值删除第一个匹配项
# - list[idx] = val 按索引修改
#
# 完成后，运行 python basic_01_列表增删改查.py 测试你的代码。
