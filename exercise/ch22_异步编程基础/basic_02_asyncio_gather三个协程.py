"""
章节：第22章 异步编程基础
题目：用 asyncio.gather 并发执行 3 个不同逻辑的协程
类型：基础练习

题目描述：
三个协程分别模拟三种常见的异步操作：
1. 查询用户信息（等 1 秒）
2. 查询订单列表（等 2 秒）
3. 写日志（等 0.5 秒）

用 asyncio.gather() 并发执行这三个协程，收集所有结果并打印。
总耗时应该约为 2 秒（三条中最长的时间），而不是 3.5 秒。

要求：
1. 定义三个 async def 函数，分别返回字典、列表、字符串
2. 用 gather 并发执行，解包结果
3. 打印结果并验证总耗时

前置准备：
无需安装额外包

提示：
- 这是真实项目中"聚合数据"场景的简化版：
  比如一个页面需要同时查用户信息、订单数据、推荐列表
  串行查需要 T1+T2+T3，异步并发只需要 max(T1, T2, T3)
- gather 返回的结果顺序 = 传入参数的顺序，与完成先后无关
- 每个协程可以用不同的逻辑，不需要统一返回值类型
"""

import asyncio
import time

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 定义 fetch_user() -> dict（1s）、fetch_orders() -> list[dict]（2s）、write_log() -> str（0.5s）
# 2. 用 user, orders, log = await asyncio.gather(...) 并发执行并解包
# 3. 用 time.perf_counter() 记录总耗时
# 4. 对比：异步并发 ≈ max(1, 2, 0.5) = 2s，串行预计 = 1+2+0.5 = 3.5s
# 5. 打印返回的用户、订单、日志结果
#
# 提示：
# - 三个协程返回值类型可以不同（dict / list / str）
# - gather 解包顺序 = 传入参数的顺序
# - 对比同步和异步的执行时间，理解数据聚合场景的加速效果
