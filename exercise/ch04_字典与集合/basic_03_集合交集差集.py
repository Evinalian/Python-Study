"""
章节：第4章 字典与集合
题目：用集合求两个列表的交集和差集
类型：基础练习

题目描述：
给定两个列表，分别求它们的交集和差集。

    list_a = [1, 2, 3, 4, 5, 5, 3]
    list_b = [3, 4, 5, 6, 7, 6]

提示：
- 先把列表转为 set 去重
- 交集用 & 运算符或 intersection() 方法
- 差集用 - 运算符或 difference() 方法
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 将两个列表转为 set
# 2. 用 & 求交集，用 - 求差集（A-B 和 B-A）
# 3. 也可尝试 | 并集和 ^ 对称差
#
# 提示：
# - set(list) 会自动去重
# - & 交集，- 差集，| 并集，^ 对称差
#
# 完成后，运行 python basic_03_集合交集差集.py 测试你的代码。
