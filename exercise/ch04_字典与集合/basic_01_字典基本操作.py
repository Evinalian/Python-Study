"""
章节：第4章 字典与集合
题目：字典基本操作
类型：基础练习

题目描述：
完成以下任务：

1. 创建一个字典 student，包含 name、age、scores（列表）
2. 添加一个新的 key "grade" 值为 "A"
3. 修改 age 为 21
4. 删除 scores
5. 用 get 安全地获取 "address"，不存在时返回 "未登记"

提示：
- 添加/修改：student["key"] = value
- 删除：del student["key"] 或 student.pop("key")
- 安全获取：student.get("key", default)
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 创建字典 student
# 2. 依次完成添加、修改、删除、安全获取操作
# 3. 每步操作后打印字典，观察变化
#
# 提示：
# - del dict["key"] 或 dict.pop("key") 删除
# - dict.get("key", default) 安全获取
#
# 完成后，运行 python basic_01_字典基本操作.py 测试你的代码。
