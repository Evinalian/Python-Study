"""
章节：第15章 正则表达式
题目：用 re.sub 替换敏感词和手机号脱敏
类型：基础练习

题目描述：
将文本中的"密码"替换为"***"，手机号中间 4 位替换为 `****`。

原始文本：
text = "我的密码是 123456，手机号是 13812345678，请不要泄露密码。"

要求：
- 第一步：用 re.sub 将所有"密码"替换为 "***"
- 第二步：用反向引用将手机号中间 4 位替换为 ****
  （分组模式：(\\d{3})\\d{4}(\\d{4})，替换为 \\1****\\2）

示例输出：
"我的***是 123456，手机号是 138****5678，请不要泄露***。"
"""

import re

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 mask_sensitive_info(text: str) -> str 函数
# 2. 第一步：re.sub(r"密码", "***", text) 替换关键词
# 3. 第二步：re.sub(r"(\\d{3})\\d{4}(\\d{4})", r"\\1****\\2", result) 手机号脱敏
# 4. 在 main 块中测试并验证结果
#
# 提示：
# - 反向引用 \\1 和 \\2 分别引用第一个和第二个分组
# - 参考第15章教程中 re.sub 和反向引用部分
#
# 完成后运行: python basic_03_re_sub替换敏感词.py
