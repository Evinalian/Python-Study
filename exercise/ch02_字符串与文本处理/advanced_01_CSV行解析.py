"""
章节：第2章 字符串与文本处理
题目：解析简单的 CSV 行（不用 csv 模块）
类型：进阶练习

题目描述：
写一个函数 parse_csv_line(line)，它接收一个 CSV 格式的字符串（逗号分隔），
返回字段列表。需要考虑引号包围的字段（字段中可能包含逗号）。

CSV 规则（简化版）：
- 字段由逗号分隔
- 字段可以由双引号包围，引号内的逗号不算分隔符
- 连续两个双引号 "" 表示转义后的双引号字符

示例输入/输出：
输入：  '张三,20,"北京,朝阳区"'
输出：  ['张三', '20', '北京,朝阳区']

输入：  '"他说""你好""",25'
输出：  ['他说"你好"', '25']

提示：
- 用状态机思想：维护一个 in_quotes 布尔标志
- 在引号内遇到逗号时当作普通字符处理
- 在引号内遇到两个连续引号 "" 时转义为一个引号
"""

# ========== TODO：请完成以下练习 ==========
#
# 步骤指引：
# 1. 实现 parse_csv_line(line) 函数，使用状态机（in_quotes 标志）
# 2. 逐字符遍历，区分引号内/外以及转义逻辑
# 3. 用给定的测试用例验证
#
# 提示：
# - 维护 in_quotes 布尔标志区分状态
# - "" 连续双引号转义为单个引号
#
# 完成后，运行 python advanced_01_CSV行解析.py 测试你的代码。


def parse_csv_line(line):
    """解析简化版 CSV 行，处理引号包围字段和引号转义"""
    pass  # TODO: 实现此函数
