"""
练习 4-2: 设计记忆提取的 prompt

设计一个 LLM prompt，用于从对话历史中自动提取值得记忆的信息。

TODO:
1. 设计记忆提取 prompt，要求：
   a) 明确定义"值得记忆"的标准：
      - 用户个人信息（姓名、职业、背景、技能）
      - 用户偏好（喜好、习惯、限制条件）
      - 确认过的事实或决策
      - 未完成的任务或待跟进事项
      - 对后续交互有帮助的经验

   b) 明确定义"不值得记忆"的标准：
      - 闲聊和问候（"你好"、"谢谢"、"再见"）
      - 一次性的临时查询（"今天天气怎么样"）
      - Agent 不确定的推测（没有经过用户确认的）
      - 纯流程性的交互（"请继续"、"再查一次"）

   c) 规定输出格式（JSON 数组，每项包含 content/type/importance/tags）
      给出一个具体的输出示例

   d) 要求 LLM 对每条记忆进行"重要度评分"（0.0-1.0），评分标准：
      - 0.9-1.0: 核心用户信息（姓名、职业），长期稳定
      - 0.6-0.8: 重要偏好或项目信息，中期有效
      - 0.3-0.5: 一般性信息或经验教训
      - 0.1-0.2: 可能有用但不紧急的信息

2. 准备测试数据：编写一段至少包含 8 轮对话的对话历史文本，其中：
   - 3-4 条信息"应该"被提取为记忆
   - 3-4 条信息"不应该"被提取为记忆
   - 包含一些边界情况（如用户说"我可能喜欢..."，这是偏好还是不确定的推测？）

3. 运行你的 prompt，分析提取结果：
   - 准确率（提取的信息中，真正应该被提取的比例）
   - 召回率（应该被提取的信息中，实际被提取的比例）
   - F1 分数
   - 分析漏提取和误提取的原因

4. 根据分析结果，改进你的 prompt，重新测试并对比改进前后的效果。
"""


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 设计记忆提取 prompt 并测试
    pass
