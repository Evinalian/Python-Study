"""
练习 2-2: 实现动态工具选择器

在 ToolRegistry 的基础上，实现一个 select_tools_by_intent 函数。

TODO:
1. 创建至少 8 个不同类型的工具，包含以下类别：
   - 信息查询类（如 search_web, query_weather）
   - 数据计算类（如 calculate, statistics）
   - 文件操作类（如 read_file, save_file）
   - 通讯类（如 send_email, send_message）
   - 每个工具都要有合适的 tags

2. 实现 select_tools_by_intent(user_message: str) -> List[Tool]:
   - 首先用 LLM 对用户消息做意图分类
   - 意图类别：信息查询 / 数据计算 / 文件操作 / 通讯 / 其他
   - 根据意图类别返回对应的工具子集
   - 对于"其他"类别，返回所有安全工具（不需要用户确认的工具）

3. 不要直接判断关键词（如 if "搜索" in message），而是用 LLM 来判断意图。
   原因：用户说"帮我查一下"可能是搜索，也可能是查数据库，关键词匹配不可靠。

4. 测试：
   - "帮我查一下北京今天的天气" → 应返回信息查询类工具
   - "计算 12345 * 67890" → 应返回数据计算类工具
   - "帮我把结果保存到文件" → 应返回文件操作类工具
   - "发一封邮件给老板" → 应返回通讯类工具
   - "你好" → 应返回所有安全工具

5. 记录每次分类的 LLM 输出，分析分类准确率。
"""


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 实现动态工具选择器并测试
    pass
