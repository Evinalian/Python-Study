"""
练习 2-3: 安全参数验证器

为 SafeToolExecutor 扩展参数验证逻辑。

TODO:
1. 在 SafeToolExecutor._validate_args 方法中添加以下检查：

   a) 字符串参数安全检查：
      - 检查是否包含路径遍历字符（如 "../", "..\\"）
      - 检查是否包含 shell 特殊字符（如 "|", ";", "&&", "$()", "`"）
      - 检查是否超过最大长度限制（从 Schema 的 maxLength 读取）
      - 如果检查失败，返回具体的中文错误信息

   b) 数值参数范围检查：
      - 检查是否在 Schema 声明的 minimum/maximum 范围内
      - 如果 Schema 中没有声明范围，跳过检查
      - 如果检查失败，返回 "参数 'xxx' 的值 yyy 超出允许范围 [min, max]"

   c) 数组参数长度检查：
      - 检查数组长度是否在 minItems/maxItems 范围内
      - 如果检查失败，返回 "参数 'xxx' 的数组长度 yyy 不在允许范围 [min, max] 内"

   d) 枚举值检查：
      - 检查参数值是否在 enum 声明的合法值列表中
      - 如果不在，返回 "参数 'xxx' 的值 'yyy' 不是合法选项。可选值: [...]"

2. 创建一个测试工具，其 Schema 包含各种约束条件，
   然后用"恶意参数"调用它，验证你的安全检查是否生效。

3. 额外挑战：实现一个 `sanitize_args` 方法，尝试自动修复一些常见的参数错误
   （如去除首尾空格、将全角数字转半角等），而不是直接拒绝。
"""


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 实现参数验证并测试
    pass
