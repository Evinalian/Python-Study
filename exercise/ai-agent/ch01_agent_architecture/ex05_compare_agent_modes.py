"""
练习 1-5（进阶）: 对比不同 Agent 模式的效率

分别用以下三种方式实现同一个任务，并对比分析。

任务: "查询上海和深圳的天气，对比两地的温差，并判断哪边更适合周末出游"

TODO:
1. 方式 A: 裸写管道（硬编码调用顺序）
   - 在代码中写死：先查上海天气 → 再查深圳天气 → 计算温差 → 输出判断
   - 不需要 LLM 参与决策，只需要 LLM 做最后的总结

2. 方式 B: ReAct Agent（第 1 章的模式）
   - 使用 run_agent 函数，让 LLM 自己决定调用顺序和次数

3. 方式 C: Plan-Execute 模式
   - 先用一个 LLM 调用生成执行计划
   - 然后按照计划逐步执行
   - 与方式 B 的区别：计划和执行是分离的

4. 对三种方式进行对比，记录以下指标：
   - LLM 调用次数
   - 总 token 消耗（可以用 tiktoken 或粗略估算）
   - 总执行时间
   - 代码行数
   - 灵活性（如果任务需求变化，修改难度如何）
   - 可维护性（其他开发者理解代码的难度）

5. 写一段 300 字以上的分析（作为注释或输出），讨论：
   - 什么情况下管道比 Agent 更合适
   - 什么情况下 Agent 比管道更合适
   - Plan-Execute 相比纯 ReAct 有什么优势和代价

提示：
- 对于方式 A，你仍然可以调用 LLM 做最后的总结，但搜索和计算步骤是代码写死的
- 对于方式 C，可以手动拆分"规划"和"执行"两个阶段，模拟 Plan-Execute 的思想
"""


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 实现三种方式并对比
    pass
