"""
练习 1-4（进阶）: 实现超时和错误重试

为 Agent 添加超时和错误重试机制。

TODO:
1. 为每个工具调用添加 5 秒超时限制
   - 使用 signal 模块（Unix）或 threading.Timer（跨平台）实现超时
   - 超时后返回 {"success": False, "error": "工具调用超时", "error_type": "timeout"}

2. 实现自动重试逻辑：
   - 如果工具返回 success=False 或超时，自动重试一次
   - 重试前等待 1 秒
   - 如果重试后仍然失败，Agent 应该向 LLM 返回错误信息，
     让 LLM 自己决定如何处理（换工具、换参数、告知用户等）

3. 额外挑战：实现指数退避重试
   - 第 1 次重试等待 1 秒
   - 第 2 次重试等待 2 秒
   - 第 3 次重试等待 4 秒
   - 最多重试 3 次

4. 测试：
   - 创建一个"前两次调用失败，第三次成功"的模拟工具
   - 验证 Agent 能正确重试并最终完成任务
   - 验证 Agent 在工具永远失败时能优雅地告知用户

提示：
- 注意线程安全（如果使用 threading）
- 思考：有副作用的工具（如发送邮件）是否应该重试？如果不应该，怎么区分？
"""

import time
import threading  # noqa


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 实现超时和重试并测试
    pass
