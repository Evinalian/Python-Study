"""
练习 3-3: 设计 Self-Ask 问题分解提示词

设计一个 system prompt，让 LLM 对复杂问题进行 Self-Ask 分解。

测试问题:
"比较 Python、JavaScript 和 Go 三种语言在 Web 后端开发中的优劣势，并给出选择建议。"

TODO:
1. 设计 system prompt，要求：
   - 包含明确的分解格式指导（如 "### 问题分解" → "### 逐一分析" → "### 综合建议"）
   - 要求 LLM 将原问题分解为至少 3 个子问题（对应三种语言的比较维度）
   - 每个子问题应该覆盖特定的比较维度（如性能、生态系统、学习曲线、并发支持等）
   - 综合回答应该基于子问题的分析结果给出明确的建议

2. 测试场景：
   场景 A: 没有搜索工具，LLM 完全靠自身知识回答
   场景 B: 有搜索工具，LLM 可以搜索每种语言的最新信息后再回答

3. 对比两种场景的输出，分析：
   - 场景 A 的回答中是否存在"幻觉"（LLM 编造了不准确的数据）？
   - 场景 B 的搜索策略是否高效（是先分解再搜索，还是边搜索边分解）？
   - Self-Ask 分解出的子问题质量如何（是否全面、是否有重叠、粒度是否合适）？

4. 记录输出并进行分析（作为注释或 print 输出）
"""


# 请在此处编写你的代码


if __name__ == "__main__":
    # TODO: 设计 Self-Ask prompt 并测试
    pass
